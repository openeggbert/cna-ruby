# Runtime capabilities

| Capability | Category | Status | Evidence |
| --- | --- | --- | --- |
| `managed.mathhelper-point-gametime` | VERIFIED_MANAGED | verified | behavior corpus and structural verifier |
| `managed.geometry-transform` | VERIFIED_MANAGED | verified complete cluster | Vector2/3/4, Quaternion, Matrix, Plane, Ray, bounds/frustum and dependency enums locally strict-zero |
| `managed.rectangle-color` | VERIFIED_MANAGED | verified complete cluster | Color and Rectangle locally strict-zero with packed/fixed-point and unchecked Int32 evidence |
| `managed.curve` | VERIFIED_MANAGED | verified complete cluster | Curve, CurveKey, collection and enum closure locally strict-zero |
| `managed.packed-vector` | VERIFIED_MANAGED | verified complete cluster | 19 types / 171 XNA identities, 68 golden observations and 262400 exhaustive iterations |
| `managed.vertex-element` | VERIFIED_MANAGED | verified complete descriptor cluster | VertexElement, VertexElementFormat and VertexElementUsage: 37 CLR / 35 Ruby identities, exact Int32 mutation, equality, smart-memory hash and string behavior |
| `managed.display-orientation` | VERIFIED_MANAGED | verified complete flags enum | DisplayOrientation: 5 CLR / 4 Ruby identities, explicit values 0/1/2/4, valid mask 0x7, controlled OR/AND and cross-type safety; no orientation application claim |
| `managed.graphics-device-status` | VERIFIED_MANAGED | verified complete non-flags enum | GraphicsDeviceStatus: 4 CLR / 3 Ruby identities, Normal=0, Lost=1, NotReset=2, ordinary-enum coercion and cross-type safety; no GraphicsDevice property or device-loss/reset behavior claim |
| `managed.graphics-profile` | VERIFIED_MANAGED | verified complete non-flags enum | GraphicsProfile: 3 CLR / 2 Ruby identities, Reach=0, HiDef=1, ordinary-enum coercion and cross-type safety; no device property, constructor, profile selection, hardware capability, or feature-level claim |
| `managed.viewport-value` | VERIFIED_MANAGED | verified complete value contract | Viewport: 14/14 identities; direct Windows XNA Project/Unproject Binary32 goldens, exact W branch and Y/depth/origin mapping, read-only TitleSafeArea copies; no GraphicsDevice setter, native projection, or renderer claim |
| `managed.clear-options` | VERIFIED_MANAGED | verified complete flags enum | ClearOptions: 4 CLR / 3 Ruby identities, Target=1, DepthBuffer=2, Stencil=4, valid mask 0x7, no named zero, and controlled typed/frozen composition; no GraphicsDevice.Clear overload or renderer clear-mask support claim |
| `managed.depth-format` | VERIFIED_MANAGED | verified complete non-flags enum | DepthFormat: 5 CLR / 4 Ruby identities, None=0, Depth16=1, Depth24=2, Depth24Stencil8=3, flags=false and value__ excluded; Depth24Stencil8 is one ordinary literal, not Depth16|Depth24; no GPU depth-format support, no stencil support, no native mapping, no DepthStencilState, and no GraphicsDeviceManager.PreferredDepthStencilFormat claim |
| `managed.primitive-type` | VERIFIED_MANAGED | verified complete non-flags enum | PrimitiveType: 5 CLR / 4 Ruby identities, TriangleList=0, TriangleStrip=1, LineList=2, LineStrip=3, flags=false and value__ excluded; XNA 4.0 renumbering, not the XNA 3.1/Direct3D 9 ordering, and no PointList or TriangleFan; no GraphicsDevice Draw* member, no vertex/index buffer, no renderer topology mapping, and no native primitive constant claim |
| `managed.pure-enum-batch.graphics` | VERIFIED_MANAGED | verified complete enum metadata contracts | Foundation 16 batch: Blend, BlendFunction, BufferUsage, ColorWriteChannels, CompareFunction, CubeMapFace, CullMode, EffectParameterClass, EffectParameterType, FillMode, IndexElementSize, PresentInterval, RenderTargetUsage, SetDataOptions, StencilOperation, TextureAddressMode, TextureFilter; 17 types / 92 Ruby identities; BufferUsage/SetDataOptions/ColorWriteChannels are the only flags enums (masks 0x1/0x3/0xF, named zero and ColorWriteChannels.All read from metadata, not invented); Blend.One=0 and Blend.Zero=1 pinned; value__ excluded. Enum metadata contract only: no renderer, GPU format, blend/depth/stencil/sampler state, render target, vertex or index buffer, Effect, adapter, presentation parameter or native mapping support is claimed |
| `managed.pure-enum-batch.audio` | VERIFIED_MANAGED | verified complete enum metadata contracts | Foundation 16 batch: AudioChannels (Mono=1, Stereo=2, no zero literal), AudioStopOptions, MicrophoneState, SoundState; 4 types / 9 Ruby identities. Enum metadata contract only. Foundation 16 opened the Audio namespace with exactly these four enums; Foundations 22 and 24 added the three Audio exception types and the two spatial descriptors, so see managed.xna-exception-types and managed.spatial-audio-descriptors for what the namespace holds now. Foundation 16 added no audio engine, SoundEffect, Microphone, XACT or CNA audio route, and claimed none; the SoundEffect cluster that exists now was built by a later milestone and is recorded under audio.sound-effect-playback |
| `managed.pure-enum-batch.media` | VERIFIED_MANAGED | verified complete enum metadata contracts | Foundation 16 batch: MediaSourceType (sparse: LocalDevice=0, WindowsMediaConnect=4), MediaState (pinned declaration order Paused=2, Playing=1, Stopped=0), VideoSoundtrackType; 3 types / 8 Ruby identities. Enum metadata contract only. This capability claims no MediaPlayer, MediaLibrary, Song, Video or playback support. Foundations 51 and 52 later added VisualizationData and Video to the same namespace from their own IL, two types this batch neither implies nor produces |
| `managed.touch-closure` | VERIFIED_MANAGED | verified complete managed value contracts | Foundation 17: TouchLocationState (Invalid=0..Moved=3), GestureType (flags, declared None=0, mask 0x3FF, no invented aggregate), TouchPanelCapabilities (get-only IsConnected/MaximumTouchCount projecting the CLR default struct value, because XNA declares no public constructor); 3 types / 17 Ruby identities. Managed contract only: that milestone implemented and claimed no TouchPanel, no TouchCollection, no gesture recognition, no touch route and no touch hardware, and queried no touch device. Foundation 23 later added the TouchLocation and GestureSample value contracts; see managed.touch-value-types, and Foundation 31 the TouchCollection pair; see managed.touch-collection |
| `managed.interface-contracts` | VERIFIED_MANAGED | verified complete abstract contracts | Foundation 18: IGameComponent, IGraphicsDeviceManager, IEffectMatrices, IEffectFog as Ruby modules whose members raise NotImplementedError; 4 types / 11 Ruby identities. IUpdateable and IDrawable joined this cluster in Foundation 20 once one CLR event gained one Ruby event-reader identity. The contracts themselves stay abstract -- every member raises NotImplementedError and no partial runtime type includes one -- and this capability adds no Effect, BasicEffect or device runtime of its own. Foundation 38 gave IGameComponent and IUpdateable their first concrete implementer, GameComponent, which includes both modules because a nominal CLR isinst needs a nominal Ruby relation and overrides every member either declares; IDrawable and IGraphicsDeviceManager still have none |
| `managed.event-projection` | VERIFIED_MANAGED | verified general mapping | Foundation 20: one CLR public event projects to one public Ruby event reader keeping the XNA spelling, answering the generic CNA::Runtime::Event subscription primitive whose whole public surface is add/remove. Registration-ordered dispatch over a snapshot, duplicate subscriptions permitted, last-occurrence removal matching Delegate.Remove, exceptions never swallowed, raising internal to the declaring implementation. System.EventArgs projects as CNA::Runtime::EventArgs with only its two public CLR identities. Verifier measures kind event under EVENT_MAPPING_MISMATCH with EVENT_IDENTITIES=6 across three owner types; mutation coverage rejects add_/remove_ pairs, writers, wrong support types, extra identities and any public emit/fire/trigger surface. No add_EventName or remove_EventName identity and no fabricated ::System namespace exists |
| `managed.event-interfaces` | VERIFIED_MANAGED | verified complete abstract contracts | Foundation 20: IUpdateable (Update, Enabled, UpdateOrder, EnabledChanged, UpdateOrderChanged) and IDrawable (Draw, Visible, DrawOrder, VisibleChanged, DrawOrderChanged); 2 types / 10 Ruby identities. Abstract contracts only: every member including the event readers raises NotImplementedError, no concrete type includes either module, and this capability adds no GameComponent, DrawableGameComponent, GameWindow or Microphone runtime of its own |
| `managed.game-component-collection` | VERIFIED_MANAGED | verified complete managed type | Foundation 35: GameComponentCollection, the sealed Collection<IGameComponent> Game.Components is; 1 type / 7 Ruby identities -- one constructor, four protected hooks and two events -- with the whole Collection<T> public surface arriving by inheritance from CNA::Runtime::Collection, which Foundation 34 projected from the admitted mscorlib. Derived from the pinned Microsoft.Xna.Framework.Game.dll IL: InsertItem refuses a duplicate before mutating and raises ComponentAdded after, RemoveItem reads before mutating and raises ComponentRemoved after, ClearItems raises every ComponentRemoved before any mutation while re-reading Count, and SetItem is an unconditional NotSupportedException. A null component is inserted silently because the IL null check guards the notification rather than the mutation. This capability holds no component: no concrete IGameComponent type exists yet and nothing populates one |
| `measurement.member-level-dependencies` | VERIFIED_MANAGED | verified measurement | Foundation 39: the IL inventory records, per type, every call/callvirt/newobj/ldftn/ldvirtftn/jmp edge that lands on a pinned reference type other than its owner -- 3271 edges across 257 types, from the same operands the native-reachability fixpoint already collected and with the same normalisation. Purely additive: TYPES_NATIVE_REACHABLE stays 77, NATIVE_ENTRY_POINT_METHODS stays 254 and no prior per-type fact moved. The frontier consumes it in two new reports and relaxes no policy: dependencyCompleteCandidates stays 19, consumableCandidates stays 0, the blocker summary is identical and selectedNext is still nil. It settles two questions the signature graph structurally cannot answer -- whether a dependency on a partial type is really unmet, member for member, and whether a type's IL reaches a type no public signature names. GameComponent reaches exactly one Game member, get_Components, which is complete, so Foundation 38's selection is proved rather than asserted; DrawableGameComponent reaches nine members of IGraphicsDeviceService, a missing type, and its Initialize throws InvalidOperationException(MissingGraphicsDeviceService) without a registered producer, so it is held back rather than looking ready; six candidates the signature graph called dependency-complete are exposed as blocked by a type only their IL reaches |
| `managed.game-component` | VERIFIED_MANAGED | verified complete managed type | Foundation 38: GameComponent, 1 type / 14 Ruby identities, the first concrete component this binding ships. Pure managed -- the IL inventory records it nativeReachable false with no native entry point -- and it really includes the IGameComponent and IUpdateable modules, which is what Game's engine tests with is_a?. Measured from the pinned Game.dll: the constructor has no null check, so a component with no Game is legal; enabled defaults true as a field initialiser and updateOrder to the CLR default 0; both setters suppress a same-value write first and write the field before raising, so a handler always observes the new value; OnEnabledChanged and OnUpdateOrderChanged declare a sender parameter and ignore it, raising with the component itself; Initialize and Update are bare rets. Dispose removes the component from Game.Components before raising Disposed, both under a reentrant lock, and the type carries no disposed flag at all, so disposing twice raises Disposed twice. Dispose(false) returns at its first instruction and Finalize is exactly that call, so the CLR finalizer does nothing observable; no ObjectSpace finalizer is registered anywhere in this binding and no GC.SuppressFinalize analogue was invented, because there is nothing to suppress. The two Dispose overloads project to one Ruby method dispatching on arity, which widens the protected overload to public -- recorded as a mapping limitation Ruby cannot avoid. Game.Dispose disposes each component over a snapshot copy. DrawableGameComponent stays deferred on Graphics.GraphicsDevice |
| `managed.game-component-engine` | VERIFIED_MANAGED | verified complete managed members | Foundation 37: Game.Components and Game.Services, the first two members any deferred partial runtime type has closed. Both are pure managed state -- each getter is one ldfld, each answers the same object for the life of the Game, neither goes through the C ABI and neither needs a native host, so both work on a Game that has never run. Services is allocated in the constructor's field-initialiser prologue and Components after the base constructor, which is the order the pinned Game.dll .ctor performs; the two collection handlers are subscribed immediately afterwards. Behind them the private engine is implemented exactly: BinarySearch with the CLR's own comparers, which answer 0 only for an equal component and so behave as a lower bound, followed by the forward walk that makes insertion stable among equal orders; order-changed subscription on insertion and unsubscription on removal; the notYetInitialized queue, drained by Game.Initialize with Count re-read and the element removed after its Initialize returns. Game.Update and Game.Draw carry their exact base bodies over a snapshot list, reading Enabled and Visible at call time. Ruby class inheritance is the whole base-call mechanism: no GameBaseUpdate-style helper exists, the native host invokes the virtual method once and never runs the base itself, and a subclass that omits super really suppresses the base component pass. Not implemented and recorded: the FrameworkDispatcher.Update the base Update performs, because the CNA game loop already drives that pump every frame and this binding's dispatcher projection requires a live Game; the device-event hookup and the conditional LoadContent in the base Initialize, both guarded by an IGraphicsDeviceService this binding has no key for; and LaunchParameters, ContentManager and GameWindow, each a missing type. Nothing registers a service and no component type ships yet |
| `managed.disposable-collapse` | VERIFIED_MANAGED | verified general mapping | Foundation 36: System.IDisposable projects to no Ruby constant at all, the second recorded structural collapse after System.IServiceProvider and the first with real reach. The admitted mscorlib shows the interface declares exactly one member, void Dispose(), and nothing else; twenty-nine XNA types declare it, twenty-eight declare a public parameterless Dispose of their own, and GraphicsDeviceManager implements it explicitly, which projects to no member under the rule ReadOnlyCollection and Collection already follow. No IDisposable or Disposable module, no Close alias, no IsDisposed of this projection's own, no finalizer API and no ownership wrapper exists; the verifier catches an invented constant under LANGUAGE_MAPPING_MISMATCH, a module inside the XNA namespaces as an INTERNAL_TYPE_LEAK, and a Close alias on a projected type as an UNEXPECTED_MEMBER. It claims no type's disposal is implemented: Audio.SoundEffectInstance and Audio.Cue lose their BCL blocker and keep NATIVE_RUNTIME, so no XACT runtime is claimed and neither becomes consumable |
| `managed.bcl-projection` | VERIFIED_MANAGED | verified general mapping | Foundation 21: CNA::Runtime::BclProjection is the measured register of non-XNA CLR identities this binding projects — System.EventArgs to CNA::Runtime::EventArgs, and System.Exception and System.Runtime.InteropServices.ExternalException to the Ruby StandardError root. The API verifier resolves and shape-checks every entry under LANGUAGE_MAPPING_MISMATCH and the dependency frontier consumes the same register, so no BCL type can be reported as mapped that the runtime does not project. StandardError rather than ::Exception because a CLR catch (Exception) is the analogue of a bare Ruby rescue; an intermediate BCL exception class the XNA surface never names collapses to the nearest projected ancestor. System.Type, IServiceProvider, IO.Stream, TimeSpan as a first-class type, Attribute, StringBuilder, SerializationInfo, ReadOnlyCollection and Dictionary are deliberately not designed |
| `managed.xna-exception-types` | VERIFIED_MANAGED | verified complete cluster | Foundation 22: six XNA exception types as real Ruby exceptions rooted at StandardError - Audio InstancePlayLimitException, NoAudioHardwareException, NoMicrophoneConnectedException; Graphics DeviceLostException, DeviceNotResetException, NoSuitableGraphicsDeviceException; 6 types / 18 Ruby identities. The pinned IL shows all eight XNA exception types declare zero fields, zero methods and four constructors whose entire body is a pure forward to the CLR exception base, with no message synthesis and no validation. The inner exception maps to Ruby Exception#cause, bound at construction with the backtrace cleared, so an unraised exception matches the CLR's InnerException set and StackTrace null. ContentLoadException and StorageDeviceNotConnectedException stay deferred on System.Runtime.Serialization. Nothing in this binding raises any of them, there is no audio engine, and Microsoft.Xna.Framework.Storage does not exist; Foundation 27 later opened the Content namespace for the five ContentSerializer attributes |
| `reference.xna-il-provenance` | VERIFIED_MANAGED | verified hash-pinned | Foundation 22: the original Microsoft XNA Framework 4.0 Windows assemblies are on this host and are located by exact SHA-256, never by filename. tools/api_compat/reference/XNA_IL_PROVENANCE.md pins ten of them by hash, size and version, including the two the behaviour corpus had cited since Milestone 3. tools/api_compat/build_il_inventory.rb admits each by hash, refuses to run on any mismatch, disassembles with ikdasm and writes docs/generated/xna-il-inventory.json: declaring assembly, IL size, declared field and method counts, per-constructor access and pure-base-forward proof, and native reachability. Native frontier 2 corrected the extractor and all 257 of 257 reference types have IL. No Microsoft-owned bytes and no machine-local path is committed, and the gem packages zero Microsoft binaries |
| `reference.bcl-authority` | VERIFIED_MANAGED | verified hash-pinned | Foundation 28: a Microsoft .NET Framework 4.0 mscorlib is admitted as a SEPARATE BCL authority beside the XNA metadata contract and the XNA IL provenance register. tools/api_compat/reference/BCL_PROVENANCE.md pins assembly mscorlib 4.0.0.0, file version 4.0.30319.1, 5196112 bytes, SHA-256 5634668d4775b0113f08ea31093b281fea69bfc4e99227f5ca761b4ed98acc63, CompanyName Microsoft Corporation, FileDescription Microsoft Common Language Runtime Class Library. tools/api_compat/build_bcl_inventory.rb admits it by exact SHA-256 and byte length, derives the public key token b77a5c561934e089 from the assembly own .publickey blob by the CLR rule rather than trusting a constant, checks the PE version resource, and proves the pairing: all ten pinned XNA assemblies declare an AssemblyRef to mscorlib naming exactly that version and token. It writes docs/generated/bcl-inventory.json with its own separate metrics, BCL_FAMILIES 3, BCL_TYPES 17, BCL_MEMBERS 246, BCL_EXCEPTION_TYPES 9. mscorlib is not an XNA assembly: REFERENCE_TYPES stays 257, REFERENCE_MEMBERS stays 2964, TYPES_WITH_IL stays 257 and TYPES_WITHOUT_IL stays 0. The inventory is demand-driven and aborts on a family the XNA reference contract does not name. No Microsoft-owned bytes, no IL text and no machine-local path is committed, and the gem packages zero Microsoft binaries |
| `reference.native-reachability` | VERIFIED_MANAGED | verified measurement | The Framework and Graphics assemblies are mixed-mode C++/CLI, so a native entry point is a P/Invoke declaration or an indirect calli through an unmanaged calling convention. 254 such methods make 77 of 257 reference types native-reachable over the assemblies own call graph. Foundation 22 measured 205 and 55 of 250 and Native frontier 2 corrected that to 214 and 61 of 257, by teaching the extractor to read a nested type declaration, which ikdasm indents inside its parent and closes with the short name, and a generic definition, which it declares as Name`1<T> and closes as Name`1. Native frontier 3 corrected it again, to 254 and 77, by removing a modopt blind spot: a mixed-mode C++/CLI thunk is declared .method public hidebysig static int32 modopt([mscorlib]...IsLong) Play(uint32), and the name reader took the first identifier before a parenthesis, so every such method was recorded as modopt while every call site resolved to its real name and every edge into it dangled. Sixteen reference types gained native reachability and none lost it, and no other measured number moved. The rule validates against the shipped boundary: every partial runtime type but GraphicsResource is native, and of the complete types exactly Input.Mouse, Input.GamePad, Graphics.Texture and FrameworkDispatcher are, each one a type whose native route this binding really implements. BEHAVIOR_EVIDENCE was retired and replaced by IL_UNAVAILABLE, NATIVE_RUNTIME and RUNTIME_DATA, which name what is genuinely absent |
| `managed.touch-value-types` | VERIFIED_MANAGED | verified complete value contracts | Foundation 23: TouchLocation and GestureSample; 2 types / 19 Ruby identities. Both constructors are pure storage with no validation; the three-argument TouchLocation form leaves prevState at the literal 0, which is Invalid; TryGetPreviousLocation fills its out value on both branches and answers [found, previousLocation]; Equals compares id, x, y, prevX and prevY while op_Equality compares all seven fields, a divergence preserved rather than normalised; every comparison is bne.un so NaN is never equal to itself; GetHashCode sums only id, x and y. GestureSample declares no Equals, GetHashCode, ToString or operator, so none is projected. No TouchPanel, TouchCollection, gesture recognition or touch device, and nothing produces either type |
| `managed.touch-collection` | VERIFIED_MANAGED | verified complete managed value contracts | Foundation 31: TouchCollection and its nested TouchCollection+Enumerator, closed together because neither can be closed alone -- the declaring type's GetEnumerator returns the nested one and the nested one's Current and MoveNext call the declaring type's Item and Count; 2 types / 18 Ruby identities. The struct declares eight TouchLocation slots and an int32 count, never a list, which is the whole eight-location ceiling. The constructor throws ArgumentNullException(touches) for null and ArgumentOutOfRangeException(touches) for more than eight, sets isConnected to the literal true, and expands each element through TouchLocation.TryGetPreviousLocation, using TouchLocationState.Invalid and a zero previous position when there is none. IsReadOnly answers the literal true and Insert, RemoveAt, Add, Clear, Remove and the Item setter are each a two-instruction body that throws NotSupportedException, mapped by managed.not-supported-exception to CNA::Runtime::NotSupportedError so an ordinary rescue catches it. The indexer constructs ArgumentOutOfRangeException(index) in its own IL and therefore raises RangeError, unlike a forwarding indexer. IndexOf scans with op_Equality over all seven TouchLocation fields, Contains is that scan negated, CopyTo throws ArgumentNullException(array) and ArgumentOutOfRangeException(arrayIndex) twice over, and FindById answers the ordered pair [found, location] filling the CLR default TouchLocation on the false branch. GetEnumerator copies the collection struct, so the enumerator walks a snapshot; it starts at -1, Current forwards to the indexer with no guard so reading it before the first MoveNext or after exhaustion raises exactly what the indexer raises, MoveNext clamps position to Count, and Dispose has a bare ret body. IEnumerator.Reset is a private explicit interface implementation outside the selected surface and is not projected. Nothing produces a TouchCollection: no TouchPanel, no gesture recognition, no touch device, and the internal Update that XNA's input layer drives is not projected |
| `managed.touch-panel` | VERIFIED_MANAGED | verified complete static contract | Foundation 32: TouchPanel, 1 type / 14 Ruby identities, closing the Input.Touch namespace. A CLR abstract sealed class, projected under the static-class rule with new raising TypeError and made private, as FrameworkDispatcher already does. The entire pinned Input.Touch assembly is a stub on the Windows profile: not one of its methods reaches a native entry point, Touch.WindowHandle is a plain static field read, and TouchPanelCapabilities.GetCaps is initobj followed by ret, so every member is settled by IL rather than by a device. GetState is the one member that needed deriving: its IL zeroes a local XNAINPUT_TOUCH_LOCATION_STATE, may call the private OnDisplaySettingsChanged, calls the internal TouchCollection.Update, and assigns the never-filled local back over prevState, so both state structs are zeroed on every call for ever, Update adds nothing and stores the literal true connected flag, and the observable result is always an empty connected collection, which TouchCollection.new([]) produces with identical state. GetState().IsConnected true beside GetCapabilities().IsConnected false is not a contradiction: the first is a field of the returned value set by the non-throwing branch and the second is the member that reports device presence, and both are XNA's Windows answer. IsGestureAvailable and ReadGesture throw InvalidOperationException, newly mapped to RuntimeError, until EnabledGestures has been assigned, and ReadGesture throws again afterwards because no gesture is ever available; both messages are localized FrameworkResources strings that are not reproduced. EnabledGestures rejects an undefined bit through GestureType.coerce, which draws the identical boundary to the CLR's 0xfffffc00 mask because the declared bits sum to exactly 0x3FF, differing only in raising RangeError rather than ArgumentException. DisplayOrientation accepts exactly one declared value, never a combination, matching Helpers.ValidateOrientation. WindowHandle is a static IntPtr nothing on this profile consumes |
| `managed.game-service-container` | VERIFIED_MANAGED | verified complete managed contract | Foundation 33: GameServiceContainer, 1 type / 4 Ruby identities, and the two BCL identities it was blocked on. The type is a managed container over one private Dictionary<Type, object>; the hash-admitted IL inventory records one public constructor, one declared field and no native reachability, so nothing in it touches CNA. Its constructor is public, so unlike most of the Game family a caller can make one and use it, but what it is for is Game.Services, and Game is one of the six deferred partial runtime types, so nothing in this binding registers a service. All three members open with the same null check throwing ArgumentNullException(type); AddService then throws ArgumentNullException(provider), ArgumentException naming type for a key already present, and ArgumentException naming no parameter when type.IsAssignableFrom(provider.GetType()) is false; RemoveService pops the Dictionary.Remove result so removing an absent key is harmless; GetService tests ContainsKey and otherwise loads null, so an absent service answers nil rather than raising. All four CLR messages are localized Resources strings and none is reproduced. System.Type projects to a Ruby Module, the type token Ruby has, which is what it is in all twenty-four places the XNA surface names it, and IsAssignableFrom is exactly is_a?. System.IServiceProvider is recorded in a new register, BclProjection::STRUCTURAL_COLLAPSE, as an identity that projects to no Ruby constant at all: it declares one member, GetService(Type), which the implementing type declares publicly, so the contract survives as that member and inventing a Ruby module would add an identity the CLR does not have. The API verifier asserts no constant was invented, and a mutation test proves the assertion fires |
| `managed.spatial-audio-descriptors` | VERIFIED_MANAGED | verified complete descriptors | Foundation 24: AudioListener and AudioEmitter; 2 types / 11 Ruby identities. Both keep state in a native-layout XACT struct but call no native code. Every Vector3 passes through FlipHandedness, which is (X, Y, -Z), on both getter and setter; the constructor stores Position and Velocity unflipped so their first read answers a negative-zero Z, while Forward and Up are flipped at construction and read back exactly. DopplerScale is guarded by bge.un, so NaN is accepted and only an ordered negative value raises. Foundation 24 added no audio engine, SoundEffect, Microphone or XACT runtime and nothing set on these two descriptors was heard by anything it built. The SoundEffect cluster that exists now consumes them through Apply3D, which is a later milestone and is recorded under audio.sound-effect-playback |
| `managed.presentation-parameters` | VERIFIED_MANAGED | verified complete descriptor | Foundation 24: PresentationParameters; 13 Ruby identities. The constructor is base() plus exactly one store, set_IsFullScreen(true), so IsFullScreen defaults to true and every other field keeps its CLR default. No setter validates anything; Bounds is a fresh Rectangle derived from the back buffer; Clone copies the whole settings struct. DeviceWindowHandle keeps the existing System.IntPtr rule - a signed native-pointer-width Ruby Integer, never a Fiddle::Pointer. It creates no GraphicsDevice, looks up no native window, enumerates no adapter, builds no swap chain and presents nothing |
| `managed.constructor-free-classes` | VERIFIED_MANAGED | verified general mapping | Foundations 25 and 26: a CLR class whose only constructor is internal projects with new made private, as GraphicsResource and Texture already did, so public non-constructibility is part of the contract while the internal path stays reachable through __send__. DisplayMode, DisplayModeCollection, ResourceCreatedEventArgs and ResourceDestroyedEventArgs completed on that rule; 4 types / 11 Ruby identities. DisplayMode derives AspectRatio, which answers zero when either extent is zero, plus TitleSafeArea and a four-field ToString; DisplayModeCollection enumerates in order and Item[format] materialises a filtered list, without mixing in Ruby Enumerable. No producer is fabricated: GraphicsAdapter and the GraphicsDevice resource events stay absent |
| `managed.content-serializer-attributes` | VERIFIED_MANAGED | verified complete cluster | Foundation 27: the five ContentSerializer attributes; 16 Ruby identities. System.Attribute projects to the CNA::Runtime::Attribute marker base, designed because five otherwise unblocked types needed it. ContentSerializerAttribute stores allowNull true before calling the base constructor; CollectionItemName answers the literal Item whenever the raw field is null or empty and its setter throws ArgumentNullException, mapped to ArgumentError, for null or empty; HasCollectionItemName is the negation of that test; Clone copies the six raw fields. This capability claims no ContentManager, ContentReader, ContentTypeReader, XNB support or content pipeline, and nothing reads an attribute. Foundation 49 later added ContentLoadException to the same namespace from its own IL, which is a sixth type this cluster neither implies nor produces |
| `managed.button-state` | VERIFIED_MANAGED | verified complete type | Released=0, Pressed=1; typed frozen non-flags enum; locally strict-zero |
| `managed.mouse-state` | VERIFIED_MANAGED | verified complete type | 14/14 identities; exact constructor, properties, equality, XOR hash, string, Int32 extremes, and independent copies |
| `managed.gamepad.enums` | VERIFIED_MANAGED | verified complete types | Buttons 25/25 Ruby identities with flags composition; GamePadDeadZone 3/3; GamePadType 10/10 including BigButtonPad=768 |
| `managed.gamepad-buttons` | VERIFIED_MANAGED | verified complete type | 17/17 identities; eleven physical properties only; XNA equality, smart-memory hash, string, copy semantics |
| `managed.gamepad-dpad` | VERIFIED_MANAGED | verified complete type | 10/10 identities; asymmetric constructor/property ordering; equality, smart-memory hash, string |
| `managed.gamepad-triggers` | VERIFIED_MANAGED | verified complete type | 8/8 identities; binary32 Math.Min/Max clamp, NaN/infinity/signed-zero, equality/hash/string |
| `managed.gamepad-thumbsticks` | VERIFIED_MANAGED | verified complete type | 8/8 identities; XNA square component clamp, NaN/infinity, nested Vector2 copies, equality/hash/string |
| `managed.gamepad-state` | VERIFIED_MANAGED | verified complete type | 15/15 identities; both constructors, Buttons[] projection, all-flags query, exact virtual thresholds, nested copies, equality/hash/string |
| `managed.gamepad-capabilities` | VERIFIED_MANAGED | verified complete type | 26/26 read-only properties, no public constructor, private independent native snapshots |
| `managed.intptr` | VERIFIED_MANAGED | verified signed native-width mapping | System.IntPtr maps to Integer; Fiddle::SIZEOF_VOIDP=8 on the qualified host; exact signed range and two's-complement native carrier |
| `managed.binary32` | VERIFIED_MANAGED | verified selected operations | Project/Unproject add direct Windows XNA Binary32 component goldens and exact x86 CLR storage grouping to the established geometry, Curve, packed, MouseState, GamePad, and VertexElement evidence |
| `native.abi-0.7` | VERIFIED_NATIVE | verified | 39 exports, 124 signatures, 290+290 layout measurements, 2 callbacks, 59 constants |
| `managed.media-video` | VERIFIED_MANAGED | verified complete managed type | Foundation 52: Media.Video, 1 type / 5 identities. Its deferral was RUNTIME_DATA -- its internal constructor takes a GraphicsDevice, one of the deferred partial runtime types, and builds a Duration from tick components the content pipeline supplies -- and both halves are about the producer. Naming a partial type in a signature is not a blocker: Foundation 40 established that a type is blocked only when its own IL calls a member of one, and this constructor merely stores the reference; and the content pipeline supplying the components says who calls the constructor, not what the type does. The pinned Video.dll shows a sealed class over seven private fields with five public get-only properties, each one ldfld, plus two assembly getters -- GraphicsDevice and Filename -- which are not identities and are not projected. The constructor is assembly, so construction is private under the Foundation 25 rule, and it stores all seven arguments with no validation. One conversion is measured rather than assumed: duration arrives as an Int32 and is stored as new TimeSpan(0, 0, 0, 0, duration), the five-argument form whose last parameter is milliseconds, so the property answers those milliseconds divided by a thousand; framesPerSecond is a float32 field and rounds to binary32. Retiring the deferral also moved the frontier: Video was Media.VideoPlayer's last unmet signature dependency, so that candidate entered the il-only blocked list. Completing it claims no MediaPlayer, MediaLibrary, Song, Album, Playlist or VideoPlayer, binds no CNA media or video route, decodes nothing and plays nothing |
| `managed.visualization-data` | VERIFIED_MANAGED | verified complete managed type | Foundation 51: Media.VisualizationData, 1 type / 3 identities. It sat on the frontier under RUNTIME_DATA, justified as filled by MediaPlayer.GetVisualizationData from live playback -- a statement about the filler rather than the type. The pinned Microsoft.Xna.Framework.dll shows a public parameterless constructor of seventy-five bytes that allocates two float[0x100] arrays, wraps each in a ReadOnlyCollection<float> over the array itself and reaches nothing at all, with both properties one ldfld each. Foundation 24 settled the same case for AudioListener and AudioEmitter, managed holders a consumer can build whose effect nothing here ever hears. Two consequences are measured rather than assumed: both collections are 256 elements long from construction with every element the CLR Single default of zero, and each is a live view rather than a snapshot, because the CLR constructor stores the array reference and takes no copy -- which is exactly what would let a filler's writes show through, and exactly what CNA::Runtime::ReadOnlyCollection projects. The CLR type argument System.Single survives as class metadata the API verifier measures under GENERIC_MAPPING_MISMATCH. Completing it claims no MediaPlayer, MediaLibrary, Song, Album, Playlist, Video or VideoPlayer, binds no CNA media route and produces no visualisation data: nothing in this binding ever fills either array |
| `managed.renderer-detail` | VERIFIED_MANAGED | verified complete value type | Foundation 50: Audio.RendererDetail, 1 type / 7 identities. It sat on the frontier under RUNTIME_DATA, justified as values that come from XACT audio renderer enumeration with no audio engine present -- a statement about the producer rather than the type. The pinned Xact.dll shows a sealed value type over two private string fields whose seven identities are field reads, ordinal string comparisons and an XOR, with not one native reach among them, so Foundation 25's rule for Graphics.DisplayMode applies unchanged: a class whose only constructor is assembly projects with construction made private, and completing it implies nothing about the enumerator that would fill it. op_Equality compares both fields with String::op_Equality, short-circuiting on the name; Equals(object) answers false for null and for a different type before delegating to it. GetHashCode contributes zero for an empty or null component and XORs the two, with the name on the left; that shape is exact, while System.String.GetHashCode is a Microsoft-internal algorithm this binding does not reproduce and a CLR hash code is documented as implementation-specific, so Ruby's own String#hash stands in and the limitation is recorded rather than hidden. ToString is declared and forwards to ValueType::ToString, which answers the type's own fully-qualified CLR name -- a deterministic string belonging to this type, not a localized resource. Completing it claims no AudioEngine, SoundBank, WaveBank, Cue, SoundEffect or Microphone, no renderer is enumerated and no CNA audio route is bound |
| `audio.sound-effect-playback` | VERIFIED_NATIVE | complete; both types projected over a behaviourally real CNA audio path | Native frontier 4 recorded this UPSTREAM_CNA_BLOCKED against the retired CNA 0.7.0 artifact. The CNA C ABI 0.21.0 migration re-measured it and three of the four negative findings do not survive. cna_sound_effect_get_sample_duration_ticks is a static computation (int32_t size_in_bytes, int32_t sample_rate, CNA_AudioChannels channels, int64_t* out_ticks) taking no handle; the audit called it with a handle and recorded its own argument error, and the 0.7.0 headers already declared the four-argument form. set_is_looped is documented settable only before playback has begun and answers CNA_RESULT_INVALID_STATE after; the audit set it last, after play and stop, and generalised that documented refusal into refused in every state. And the control that excluded the null backend was inert: CNA never reads CNA_AUDIO, the audio platform is a build-time CMake selection (cmake/AudioPlatformSelection.cmake, CNA_AUDIO_PLATFORM=SDL3|SDL2|NULL) and the retired artifact was built NULL. Re-measured against that same 0.7.0 artifact with corrected prototypes, cna_audio_get_capabilities answers is_playback_available=0 and both duration routes report CNA_RESULT_SUCCESS without writing their output -- the artifact really had no audio, for the reason the audit had ruled out. On the current SDL3-platform/SDL3-audio artifact one full second of mono PCM16 at 44100 Hz answers exactly 10000000 ticks from both duration routes, SoundState moves Stopped -> Playing -> Paused -> Playing -> Stopped through the canonical routes, IsLooped round-trips before play and is refused after, and volume, pitch and pan round-trip. Audio.SoundEffect and Audio.SoundEffectInstance are now projected on that path, 2 types / 33 identities over 35 bound routes, and every bound is a literal read out of the pinned IL rather than a documented range: sampleRate in [8000, 48000] from ldc.i4 0x1f40/0xbb80, channels in [1, 2], MasterVolume and Volume in [0, 1], Pitch and Pan in [-1, 1], DopplerScale >= 0, SpeedOfSound > 0, and DistanceScale >= 0 then clamped up to Single.Epsilon. Every one of those comparisons is unordered, so a NaN takes the throw branch -- except DistanceScale's bge.un, which takes the branch past it, so DopplerScale refuses NaN and DistanceScale accepts it, an asymmetry that is the IL's and not this projection's. Two state rules the IL settles and no signature says: set_Pan clears the 3D flag unless a packet has been submitted, so Apply3D before the first play does not lock Pan out and after it does; and UnsafeApply3D is its mirror image, so a first 3D call is allowed before playback and refused after while a second on an already-3D instance is always allowed. CNA enforces both rules natively and correctly, and the managed guards are kept anyway so each refusal carries XNA's InvalidOperationException identity rather than a translated native error. The three instance setters are validated managed-side because CNA's three disagree with each other -- volume unclamped, pitch clamped, pan refused -- and managed validation is the only arrangement under which they behave alike, which is what XNA's contract says. THREE DEVIATIONS: the four global settings are game-scoped where XNA's are CLR statics, because every canonical route takes a game handle, the same asymmetry FrameworkDispatcher records; a SoundEffect needs a live CNA Game to exist where XNA's constructor needs nothing, with GetSampleDuration and GetSampleSizeInBytes the only two members that work without one; and State is CNA's SoundState directly rather than XNA's mapping of an XACT VoiceState bitfield that does not exist here. Ownership is enforced by CNA and asserted rather than papered over: destroying an effect with a live instance answers CNA_RESULT_INVALID_STATE. NO AUDIBLE OUTPUT IS CLAIMED: no listener and no capture device took part in any of this, and no automated measurement on this host can establish it either way. This claims no AudioEngine, SoundBank, WaveBank, Cue, AudioCategory, Microphone, DynamicSoundEffectInstance or Media runtime |
| `managed.serialization-carrier` | VERIFIED_MANAGED | verified complete managed types | Foundation 49: the System.Runtime.Serialization carrier and the two XNA exception types it unblocks -- Content.ContentLoadException and Storage.StorageDeviceNotConnectedException, 2 types / 8 identities over two newly projected BCL identities. Both were deferred through eight milestones as a BCL cluster no otherwise-unblocked type needs; what that cluster costs is two nominal identities. They are nominal rather than markers because of a Ruby fact: both types declare two two-argument constructors, (message, innerException) and (info, context), and Ruby has no overload by parameter type, so one initialize must tell them apart on a real first-argument type. Each carries what the XNA surface can reach and no more, by the same narrowness rule that maps System.Type to Module: the surface names both and calls no member of either, since all four constructors are pure forwards. SerializationInfo carries the general AddValue/GetValue pair and MemberCount with mscorlib's exact validations -- ArgumentNullException on a null name, SerializationException on a duplicate, SerializationException on a missing member, a stored nil being a value rather than an absence -- while its other forty public members are typed overloads a dynamically typed language cannot distinguish plus a formatter's type-resolution protocol no formatter here runs. StreamingContext is the sequential sealed value type it is in the IL, with value equality; StreamingContextStates is projected as its flags enum but deliberately not registered, because the selected reference never names it. SerializationException joins the thrown-exception register as a dedicated StandardError subclass. System.Exception's serialization constructor reads eleven named values and Ruby's exception base holds one, the message, so that is the one that crosses and the other ten are not fabricated -- asserted by filling a carrier with all of them. The projection also unifies what Foundation 46 left duck-typed: Dictionary's GetObjectData and OnDeserialization now take the same nominal carrier. Nothing here serialises anything: no formatter, surrogate selector, binder, stream format or ISerializable module, and neither exception is raised by anything |
| `managed.dictionary-launch-parameters` | VERIFIED_MANAGED | verified complete managed type | Foundation 46: CNA::Runtime::Dictionary and Microsoft.Xna.Framework.LaunchParameters, 1 XNA type / 1 Ruby identity over an eleventh projected BCL identity. The last purely-decisional BCL blocker, decided by measurement rather than preference. The admitted mscorlib settles how much of Dictionary`2 a projection owes: twenty-two of its methods are explicit interface implementations, which project to no member at all under the rule ReadOnlyCollection's twelve and Collection's fourteen already follow, and its SerializationInfo constructor is family. What is carried is the ordinary public surface -- Comparer, Count, Keys, Values, the indexer, Add, Clear, ContainsKey, ContainsValue, GetEnumerator, Remove, TryGetValue, GetObjectData and OnDeserialization -- plus the nested KeyCollection and ValueCollection, whose whole public surface is four members each. A Ruby Hash is the private store and never the public projection: Hash#[] answers nil where get_Item throws KeyNotFoundException, Hash#store overwrites where Add throws ArgumentException, and Hash#each never fails fast on a version counter. Measured exactly: the null-key refusal every keyed member shares, ContainsValue's separate null branch, Remove answering false without touching version, and Clear returning at its first branch so an empty Clear does not invalidate an enumeration. Bucket traversal order is deliberately not reproduced -- it is unspecified in the contract and the canonical C ABI's own launch-parameter enumeration sorts by name rather than trusting it -- while key equality, duplicate recognition, lookup, removal, Count and version invalidation are exact. The default comparer projects to nil because EqualityComparer<TKey>.Default is the key's own equality, which is Ruby's; a supplied comparer is a duck type answering Equals and GetHashCode, which is the whole of IEqualityComparer<T>, and no implementation-private CLR comparer class is exposed. GetObjectData and OnDeserialization take a named-value carrier: no formatter, surrogate selector, binder or stream format is invented, and SerializationInfo stays out of the register because the pinned reference never names it in a public signature. LaunchParameters declares one member, its public parameterless constructor, and parses ARGV with XNA's own rules -- leading / and - trimmed as a run, split on the FIRST colon, a colonless argument KEPT with String.Empty as its value, an empty key skipped, first occurrence winning. Game.LaunchParameters is one ldfld, created by the constructor, needing no native host. The canonical CNA launch-parameter routes are deliberately unbound: audited against this IL they skip a colonless argument, upsert where Add refuses, and enumerate sorted by name, all observable through the inherited surface. Nothing here serialises anything and no content, window or storage runtime is implied |
| `managed.game-protected-remainder` | VERIFIED_MANAGED | verified complete managed members | Foundation 47: Game's Dispose(Boolean), Finalize and ShowMissingRequirementMessage(Exception), the three family members that were the whole of its remaining decisional surface. Dispose(Boolean) opens `ldarg.1; brfalse` straight to `ret`, so Dispose(false) does nothing and Finalize -- which is exactly that call in a try/finally around base.Finalize -- is observably a no-op; the two overloads project to one Ruby method dispatching on arity, widening the protected one to public as a recorded mapping limitation. The Monitor.Enter(this) that Foundation 41 recorded as a deviation is now taken, with ::Monitor rather than Mutex because the CLR lock is reentrant and a Disposed handler re-entering it must not deadlock. UnhookDeviceEvents is a genuine no-op rather than one made into one: its field is written only by HookDeviceEvents, which the producer audit deliberately omits. XNA's absent disposed flag remains a recorded deviation, because native destruction is not repeatable. ShowMissingRequirementMessage is host?.ShowMissingRequirementMessage(e) ?? false; GameHost's own body is an unconditional false and only WindowsGameHost overrides it with a System.Windows.Forms MessageBox, so answering false here is the abstract base's real behaviour and the truthful answer, not a stub -- and RunGame rethrows on false, so the exception still reaches the caller. The two catch clauses are wired and live: a subclass raising either projected requirement exception from a lifecycle callback reaches them, the default rethrows, an override answering true really suppresses, and an unrelated exception is not caught. RunGame's finally clearing inRun is now in Run, so the flag survives a run that ends by raising. This claims no finalizer, no ObjectSpace registration, no GC.SuppressFinalize analogue, no message box, no dialog and no CNA route |
| `native.game-window` | VERIFIED_NATIVE | verified canonical routes | Foundation 48: GameWindow, 1 type / 20 identities, and the Game.Window it closes. It sat on the frontier under RUNTIME_DATA with the justification that projecting it would require the deferred Game/window runtime; that was wrong in the way FrameworkDispatcher's deferral was, because the concrete implementation this binding needs is CNA's and the canonical C ABI supplies every member XNA leaves abstract. Thirteen routes bound, and every one is addressed through the game handle -- CNA's window has no handle of its own -- so this is a façade over the host with no second lifetime and no producer question. Game.get_Window is host?.Window whose null branch is unreachable in XNA because its constructor makes the host, so this completes that deferred step rather than exposing a nil XNA never shows. Title forwards rather than shadowing: the abstract constructor sets String.Empty but WindowsGameWindow immediately calls set_Title(GetDefaultTitleName()), so the observed default is the host's, and CNA's arrives through CNA_GameCreateInfo::window_title -- measured round-tripping UTF-8. Both of set_Title's validations are kept, the null refusal and the same-value suppression. Six of XNA's event fields are internal, so only three are identities, and the ABI defines exactly those three. Measured HEADLESS answers: a zero client rectangle, a zero native handle, an empty screen device name and Default orientation -- the host's honest report of a platform with no native window, with AllowUserResizing and Title round-tripping through the real routes to prove the rest are answers rather than placeholders. SetSupportedOrientations has no canonical route and refuses rather than pretending. The CNA_StringView by-value parameter is decomposed into the two eightbytes the System V x86-64 classification puts in registers, recorded on the signature and reconstructed by the ABI probe rather than smuggled. This claims no visible window: nothing creates, shows, moves, resizes or paints one, no System.Windows.Forms surface or message pump is added, and nothing in the reviewed artifact raises a window event |
| `native.game.tick` | VERIFIED_NATIVE | verified canonical route | Foundation 44: Game.Tick, bound to cna_game_tick, the frame step cna_game_run_one_frame wraps. The pinned Game.dll IL makes the split XNA's own -- Game.RunOneFrame is host?.RunOneFrame(), and WindowsGameHost implements that as gameWindow.Tick(), then GameHost.OnIdle() whose only subscriber is Game.HostIdle whose whole body is this.Tick(), then the Guide-visibility relay -- so the two are bound separately and a regression test asserts they never become aliases. Tick's first instruction, if (ShouldExit) return, is projected in managed state because exitRequested is written exactly once in the whole assembly, by Game.Exit, and never cleared, while CNA's step after cna_game_request_exit still delivers one Update. Measured faithful over the reviewed artifact: the fixed step advances TotalGameTime by exactly one TargetElapsedTime per tick, a pending SuppressDraw skips exactly one draw, and Tick delivers no lifecycle callback other than Update/BeginDraw/Draw/EndDraw. Recorded deviations: CNA refuses a frame step from inside a lifecycle callback, which surfaces as its own translated error, and the route is owner-thread bound. Two CNA loop deviations were measured and deliberately not re-implemented on the managed side -- the missing doneFirstUpdate draw gate and the missing priming Update in Run -- because the native Game owns the loop and a managed copy would double a step CNA performs |
| `native.game.is-active` | VERIFIED_NATIVE | verified canonical route | Foundation 45: Game.IsActive, the exact pinned expression rather than the backing field. Game.get_IsActive is thirty bytes and reads isActive && !(GamerServicesDispatcher.IsInitialized && Guide.IsVisible); each term has exactly one canonical CNA route -- cna_game_get_is_active, cna_gamer_services_dispatcher_get_is_initialized and cna_guide_get_is_visible -- all three already exported and none previously bound. The two GamerServices routes take no handle, which is the shape of the CLR statics they project, and are measured to answer with no Game in the process and off the owner thread, so the IL's evaluation order is kept literally. cna_game_get_is_active tracks XNA's field exactly: false before the loop and already true inside the Activated handler, which is where HostActivated leaves it. isActive is host-written and consumer-read, so it forwards rather than being mirrored, and raising Activated by hand does not make the game report itself active. The guide term is asked, never assumed: in this artifact it answers false and cna_guide_set_is_visible is accepted without being reflected, so the branch is proved wired by a six-row truth table over the private route readers rather than exercised. This claims no GamerServices runtime: the selected profile carries exactly one GamerServices type, GamerServicesComponent, which is still missing, and neither Guide nor GamerServicesDispatcher is a selected identity. Recorded deviations: owner-thread binding once a host exists, and a disposed Game raising rather than answering the stale value XNA's field would still hold |
| `native.game-lifecycle` | VERIFIED_NATIVE | verified | Initialize/LoadContent/BeginRun/Update/Draw/EndRun/UnloadContent native ordering |
| `native.callback-exceptions` | VERIFIED_NATIVE | verified | Initialize, LoadContent, Update, Draw containment tests |
| `native.graphics-clear` | VERIFIED_NATIVE | verified HEADLESS | real callback-borrowed device Clear |
| `native.texture2d-stream` | VERIFIED_NATIVE | verified HEADLESS | project PNG decoded as measured 128x128 Texture2D |
| `native.spritebatch-draw` | VERIFIED_NATIVE | verified HEADLESS | real Begin/scaled Draw/End |
| `native.keyboard` | VERIFIED_NATIVE | verified HEADLESS | CNA state capture and typed key queries |
| `native.mouse.get-state` | VERIFIED_NATIVE | verified HEADLESS | real cna_mouse_get_state snapshots copied into distinct managed MouseState values; 50 stress calls |
| `native.mouse.set-position` | VERIFIED_NATIVE | verified canonical route | real cna_mouse_set_position call with exact Int32 validation; HEADLESS does not qualify physical cursor movement or round trip |
| `native.mouse.window-handle.get` | VERIFIED_NATIVE | verified canonical route | real cna_mouse_get_window_handle call; zero/null and signed IntPtr conversion qualified |
| `native.mouse.window-handle.set` | VERIFIED_NATIVE | verified canonical route | real cna_mouse_set_window_handle call; safe same-token/zero operation qualified; CNA never owns or frees the external token |
| `native.mouse.thread-generation` | VERIFIED_NATIVE | verified | owner-thread rejection and retry; shutdown rejection; Game-1 to Game-2 context reselection; central native error propagation |
| `native.gamepad.get-state.basic` | VERIFIED_NATIVE_ROUTE | verified canonical route | real cna_gamepad_get_state for all four player slots; HEADLESS returned CNA disconnected snapshots with real packet field; connected positive path HARDWARE_PENDING |
| `native.gamepad.get-state.none` | VERIFIED_NATIVE_ROUTE | verified canonical route | real cna_gamepad_get_state_with_dead_zone(None); exact XNA clamping algorithm reviewed; nonzero analog hardware evidence HARDWARE_PENDING |
| `native.gamepad.get-state.independent-axes` | VERIFIED_NATIVE_ROUTE | verified canonical route | default and explicit IndependentAxes routes executed; XNA thresholds left=7849/32767, right=8689/32767, trigger=30/255; boundary behavior managed-verified |
| `native.gamepad.get-state.circular` | VERIFIED_NATIVE_ROUTE | verified canonical route | real Circular route executed; exact radial XNA algorithm reviewed; nonzero analog hardware evidence HARDWARE_PENDING |
| `native.gamepad.get-capabilities` | VERIFIED_NATIVE_ROUTE | verified canonical route | real cna_gamepad_get_capabilities for all four slots; each of 26 properties explicitly mapped; positive flags HARDWARE_PENDING |
| `native.gamepad.set-vibration` | VERIFIED_NATIVE_ROUTE | verified canonical route | real cna_gamepad_set_vibration all-off request returned backend false; XNA UInt16 conversion retained; positive motor actuation HARDWARE_PENDING |
| `native.gamepad.thread-generation` | VERIFIED_NATIVE | verified | all three routes reject wrong thread before native entry; shutdown/ambiguity rejection; Game-1 to Game-2 reselection; central error propagation |
| `native.framework-dispatcher` | VERIFIED_NATIVE_ROUTE | verified canonical route | real cna_framework_dispatcher_update executed inside Update and Draw callbacks and outside any callback, repeated without accumulation; documented preconditions qualified (no live Game and off-owner-thread both refuse before the native entry, disposed Game refuses). Native frontier 1 recorded that the pinned XNA Windows IL reaches no native entry point here, because PollForEvents is an empty method. The premise was right and the conclusion wrong: Native frontier 3 removed a modopt blind spot in the extractor and the drain is native-reachable after all, through SoundEffect.RecycleStoppedFireAndForgetInstances into XACT, so Update ends in native work in XNA too and forwarding to the canonical CNA pump is the closer analogue rather than the looser one. The type declares no native entry point of its own; the reachability is transitive. No managed sink observes the drain, because none of the five the IL dispatches to has a Ruby projection |
| `native.lifecycle-stress` | VERIFIED_NATIVE | verified | 20 Game/Texture/SpriteBatch/recreation cycles, 50 Mouse and 50 GamePad state calls, 20 GamePad capability calls; zero observed crash/UAF/double-free |
| `mouse.physical-cursor` | BACKEND_BLOCKED | not verified | qualified artifact uses HEADLESS; no visible cursor or SetPosition/GetState physical round trip is claimed |
| `mouse.nonzero-window-handle` | PLATFORM_PENDING | not exercised | HEADLESS safely qualifies zero and same-token get/set, not an arbitrary real desktop window |
| `gamepad.connected-state` | HARDWARE_PENDING | not exercised | no controller connected on the qualified host; disconnected native-negative routes are verified |
| `gamepad.positive-capabilities` | HARDWARE_PENDING | not exercised | HEADLESS/no-controller snapshots honestly report Unknown and false; no positive device flags claimed |
| `gamepad.physical-vibration` | HARDWARE_PENDING | not exercised | canonical route returned false with no controller; no nonzero or looping rumble request was issued |
| `gamepad.voice-support-positive` | PLATFORM_PENDING | not exercised | CNA exposes the real per-device voice capability; qualified Linux platform returned false with no controller |
| `renderer.visible-output` | HARDWARE_PENDING | not shown on a physical display | Native frontier 6 retired this row's reason, not the row. It read "qualified artifact uses HEADLESS renderer", and that stopped being true: docs/generated/renderer-native-report.json carries a second qualified artifact, CNA_GRAPHICS_RENDERER=OPENGL33, whose EasyGL family reports OpenGL 4.5 Core through Mesa, creates a real X11 window and reads a cleared render target back exactly. What is still not verified is the word visible: the qualification runs against an Xvfb framebuffer, so the pixels are real and rasterised by a real driver but no physical monitor has displayed them. See renderer.real-rasterization for what is verified and hardware.visible-renderer for the remaining question |
| `renderer.real-rasterization` | VERIFIED_NATIVE_RENDERER | verified; a render target is cleared and read back exactly | docs/generated/renderer-native-report.json, runs.OPENGL33.renderTarget. An 8x4 RenderTarget2D is created through cna_render_target2d_create, reports renderer_available true, is bound with cna_graphics_device_set_render_target2d, cleared to (0.25, 0.5, 0.75, 1.0) with cna_graphics_device_clear_rgba, unbound, and read back through cna_texture2d_get_data -- the same route Texture2D.GetData uses -- as 32 identical pixels of exactly (64, 128, 191, 255). The HEADLESS run performs the identical sequence, reports success from create, bind, clear and unbind, and then answers CNA_RESULT_NOT_SUPPORTED from the readback, which is why no earlier milestone could make this claim. Measured with it: the same probe run from LoadContent rather than Draw reads back transparent black while every call still reports success, because a clear issued outside the renderer's frame has no target to land on |
| `renderer.native-window` | VERIFIED_NATIVE_RENDERER | verified; a real X11 window with a real XID | runs.OPENGL33.renderer: cna_game_window_get_native_window_ext reports CNA_NATIVE_WINDOW_SYSTEM_X11 with a non-null Display* and a real window XID, client bounds 800x480 and the display's own name. The HEADLESS run answers CNA_NATIVE_WINDOW_SYSTEM_UNKNOWN with every pointer null and a zero rectangle, which CNA's own comment states is deliberate for the four renderers whose descriptors set needsWindow = false -- HEADLESS, SOFTWARE, STUB and PORTABLEGL. SOFTWARE was measured and is one of them: selecting it changes the renderer name and the capability flags and still creates no window, so "a renderer that is not HEADLESS" is not the same question as "a renderer that has a window" |
| `renderer.frame-stability` | VERIFIED_NATIVE_RENDERER | verified; 60 and 600 frames on both qualified artifacts | runs.*.frames. One game, a real GraphicsDevice.Clear in every Draw, run to 60 and to 600 frames against both the HEADLESS and the OPENGL33 artifact: 60/60 and 600/600 completed with no error on each, the OPENGL33 run presenting through a real GL context and X11 window each frame. Measured alongside it, and not a binding defect: creating and destroying tens of games in one process against a windowed renderer intermittently fails inside SDL with "x11 not available", because each game acquires and releases the video subsystem; one long-lived game is unaffected |
| `graphics.volume-and-cube-textures` | VERIFIED_NATIVE_RENDERER | complete; both types round-trip real texels on a renderer that has the storage | Graphics.Texture3D (11 identities) and Graphics.TextureCube (9), the two types EffectParameter's GetValueTexture3D and GetValueTextureCube return. Ten canonical routes and six layouts, ABI_MISMATCHES=0. Under OPENGL33 sixteen voxels and six distinct cube faces round-trip byte for byte, including a one-texel sub-rectangle of one face and a one-slice box of the volume; under HEADLESS cna_texture3d_create is refused outright (CNA_GRAPHICS_CAPABILITY_TEXTURE_3D answers false) and cna_texturecube_set_data refuses to store a face, so the storage claim is made only where it was measured. The managed contract -- XNA's own constructor refusals in the IL's order, GetAndValidateBox's unsigned comparisons, GetAndValidateRect, ValidateTotalSize and the empty-array-is-ArgumentNullException rule -- is asserted against both artifacts. DEVIATION, recorded: both CNA transfer routes take const CNA_Color*, so an element type XNA would accept is refused with NotSupportedError after the managed validation has run |
| `graphics.effect-cluster` | VERIFIED_NATIVE_RENDERER | complete; nine types over a real compiled effect, and the type they finished | Effect, EffectParameter, EffectAnnotation, EffectPass, EffectTechnique and their four collections -- 96 identities -- plus SpriteBatch.Begin's two Effect-taking overloads, which completed that type and took the partial register from three to two. Sixty-four routes, twenty-eight constants and seven layouts, ABI_MISMATCHES=0. Effect's producer is compiled Effect Framework bytecode, and cna_effect_create_compiled accepts it only where CNA_GRAPHICS_CAPABILITY_COMPILED_EFFECTS is true -- the MojoShader runtime is a fetched dependency the EasyGL, SDL_GPU and Vulkan families carry only when their build option is on. One of the three qualified artifacts has it, the OPENGLES3 build with CNA_EASYGL_COMPILED_EFFECTS=ON, and against it a real .fxb reflects six parameters covering every EffectParameterClass and two techniques: values round-trip, the scalar-to-vector broadcast the IL specifies is observed, InvalidCastException guards fire on the wrong reader, EffectPass.Apply refuses a pass outside the current technique with XNA's own exception after calling OnApply, Clone produces an independent graph and a sprite really draws through the effect with a transform. Object identity is preserved where XNA makes it observable, which needs a cache: every CNA getter here returns a **fresh owned view**, measured -- two get_at(0) calls answer two handles naming one parameter -- and a fresh native handle is not a new XNA object. Under the other two artifacts the structure is fully asserted and every behavioural test skips with its reason |
| `graphics.viewport-setter` | UPSTREAM_CNA_BLOCKED | unbound | CNA prototype passes CNA_Viewport by value; Fiddle foundation declines unsafe declaration |
| `struct.assignment-copy` | LANGUAGE_MAPPING_LIMITATION | documented | Ruby assignment aliases; copies enforced at binding boundaries |
| `content.xnb` | UNIMPLEMENTED_CNA_RUBY | deferred | fake ContentManager removed |
| `effects.3d-model` | UNIMPLEMENTED_CNA_RUBY | deferred | managed Matrix is qualified; fake BasicEffect/cube support remains removed |
| `audio-media` | UNIMPLEMENTED_CNA_RUBY | the Audio namespace is complete; the Media runtime is not | Managed contracts only. Audio holds AudioChannels, AudioStopOptions, MicrophoneState, SoundState, InstancePlayLimitException, NoAudioHardwareException, NoMicrophoneConnectedException, AudioListener, AudioEmitter and RendererDetail, the last added by Foundation 50 as a sealed value type over two strings whose producer -- an XACT AudioEngine -- is still absent; Media holds MediaSourceType, MediaState, VideoSoundtrackType, VisualizationData and Video, the last two added by Foundations 51 and 52 -- VisualizationData as a constructible holder of two 256-element zero-filled collections whose filler -- MediaPlayer.GetVisualizationData -- is still absent. Every audio and media runtime remains absent and unimplemented: no engine, no device, no playback, no XACT, no MediaPlayer or MediaLibrary, and nothing that produces visualisation data. Exactly one canonical CNA route touches this subsystem: FrameworkDispatcher.Update forwards to cna_framework_dispatcher_update, which drains CNA own pending audio and media work. The drain is real; its managed fan-out reaches nothing here, because none of the five XNA sinks the pinned IL dispatches to has a Ruby projection. Nothing here makes a sound, plays anything or reads a device. Nothing is deferred in Audio any more. Audio.SoundEffect, Audio.SoundEffectInstance, Audio.DynamicSoundEffectInstance, Audio.Microphone, Audio.AudioEngine, Audio.AudioCategory, Audio.WaveBank, Audio.SoundBank and Audio.Cue are all COMPLETE over real CNA paths, beside the managed Audio.AudioListener, Audio.AudioEmitter, Audio.RendererDetail, the four Audio enums (AudioChannels, AudioStopOptions, MicrophoneState, SoundState) and the three Audio exception types, each recorded in its own row. Media.VisualizationData, Media.Video and Media.MediaSource are complete too -- the last of them because XNA's own GetAvailableMediaSources queries nothing -- so what stays deferred under this row is the Media *runtime*: MediaLibrary, MediaPlayer, MediaQueue, Song, Album, Artist, Genre, Playlist, Picture and VideoPlayer, with their collections. Nothing here claims audible output |
| `managed.media-source` | VERIFIED_MANAGED | complete; four identities, none of which reaches anything | Microsoft.Xna.Framework.Media.MediaSource, 1 type / 4 identities, and the last entry the frontier's RUNTIME_DATA register ever held. The deferral read "GetAvailableMediaSources enumerates the host media sources; no media stack has been queried" -- the seventh deferral to describe a producer rather than the type, and the plainest wrong one, because the producer does not exist. The method is ldc.i4.1; newarr; newobj; stelem.ref; ret: a one-element array holding one object whose constructor stores MediaSourceType.LocalDevice and one resource string. XNA queries no media stack either. The register is empty now. The type needs no Game and reaches no native entry point, which the IL inventory measures. Name is FrameworkResources.WmpMediaSource, extracted from the pinned assembly's own resource table -- entry WmpMediaSource, string record at data-section offset 22969 -- and is "Local Windows Media Player library" rather than a guess. DEVIATION: that is a localized resource, so XNA's answer depends on the UI culture and the qualified profile is the en-US assembly; CNA's cna_media_source_copy_name_at answers "Local Device" for the same source, and a test asserts both, that CNA reports one source too, and that the two agree it is a LocalDevice. The four cna_media_source_* routes are bound to measure that divergence rather than to answer the question, because XNA answers it from a resource string. Completing it implies no MediaLibrary, MediaPlayer, Song, Album, Artist, Playlist, Picture, MediaQueue or VideoPlayer, and no route for any of them is bound. |
| `input.touch` | VERIFIED_MANAGED | verified complete managed namespace | Managed contracts only, and the whole Input.Touch namespace is now complete: TouchLocationState, GestureType, TouchPanelCapabilities, TouchLocation, GestureSample, TouchCollection, TouchCollection+Enumerator and TouchPanel. Nothing in it reads a device, and that is a measurement rather than a policy: the hash-admitted IL inventory records nativeReachable false for every type the pinned Microsoft.Xna.Framework.Input.Touch.dll declares, because XNA 4.0's touch support was for Windows Phone and the Windows assembly answers constants. TouchPanel.GetCapabilities forwards to TouchPanelCapabilities.GetCaps, whose whole body is initobj, so it answers IsConnected false and MaximumTouchCount 0; GetState always answers an empty connected TouchCollection because both native state structs its IL passes to the internal TouchCollection.Update are zeroed on every call; IsGestureAvailable answers the literal false once gestures have been enabled and throws InvalidOperationException before that; and ReadGesture throws on both branches and cannot return. No touch hardware has been measured, nothing queries a touch device, no touch route was bound, and no CNA symbol was added |
| `evidence.behavior-corpus-upstream-source` | UPSTREAM_INPUT_UNAVAILABLE | not available on this host | the upstream behaviour-corpus source with SHA-256 398d0201af0e3c719c152f8659a871cb59710a7dfafc079df6694d453c737855 is not on this host, so tools/import_behavior_corpus.rb has never been run. Corpus additions are merged by the documented deterministic replay, which refuses to write unless re-serialising the pre-merge corpus reproduces its bytes exactly, and every merge and correction to date has been proved additive or element-wise unchanged |
| `platform.windows` | PLATFORM_PENDING | not qualified | — |
| `platform.macos` | PLATFORM_PENDING | not qualified | — |
| `platform.browser-wasm` | PLATFORM_PENDING | unsupported | no CNA C-ABI Wasm/Ruby-Wasm architecture |
| `platform.android-mruby` | PLATFORM_PENDING | unsupported | not part of MRI desktop qualification |
| `hardware.visible-renderer` | HARDWARE_PENDING | not run on an attached display | Native frontier 6 qualified CNA_GRAPHICS_RENDERER=OPENGL33 against an Xvfb X11 display with Mesa's software rasteriser. Running the same artifact against this host's attached display and a hardware GL driver is an environment question this suite deliberately does not take: it would put a window on the developer's desktop and make a qualification result depend on which GPU is present |
| `assets.content-pipeline` | ASSET_PENDING | not started | only project-owned PNG stream path qualified |
| `managed.readonly-collection` | VERIFIED_MANAGED | verified projection | Foundation 29 resolved the decision mapping.readonly-collection recorded. System.Collections.ObjectModel.ReadOnlyCollection`1 projects to CNA::Runtime::ReadOnlyCollection, a dedicated runtime support class, and never to Array, a frozen Array, Enumerable alone, Set or an opaque Object. The mscorlib IL admitted by reference.bcl-authority settles the design rather than taste: the CLR constructor stores the IList<T> reference and all six public read members forward one call to it, so the wrapper is a live view whose Count and contents follow the backing list, and a frozen copy would be a different type; none of the six validates anything of its own; and read-only is a property of the interface, expressed by twelve explicit interface implementations that throw NotSupportedException unconditionally, which Ruby projects as the absence of any mutating member. Ruby has class inheritance, so an XNA class whose CLR base is this generic inherits from the support class; because a Ruby class is not statically generic the CLR type argument is carried as class metadata via projects_elements and measured by the API verifier under GENERIC_MAPPING_MISMATCH. The class carries one language-support identity of its own, each, and includes Enumerable, every method of which Ruby derives from each alone; CLR identities are PascalCase or operators and language support is not, so the two can never be confused. The projection completes no XNA type: GraphicsAdapter drops to NATIVE_RUNTIME alone and Media.VisualizationData to RUNTIME_DATA alone, while Microphone keeps System.Byte[] and SpriteFont keeps System.Char, Nullable`1[System.Char] and StringBuilder |
| `display.adapter-enumeration` | UPSTREAM_CNA_BLOCKED | blocked by a measured native ordering defect, not by the renderer | The blocker this row recorded -- "the qualified artifact was linked with the HEADLESS platform, so CNA GetDisplays answers nothing" -- was measured again in Native frontier 6 against an artifact that has the SDL3 platform, the OPENGL33 renderer, a real X11 window and a live 1280x800 display, and it is still the no-display fallback: one adapter described "Default Display", device name \\.\DISPLAY1, a single fabricated 800x480 mode. The cause is ordering rather than capability. GraphicsAdapter::getAdaptersProperty() fills a static cache on first use; GraphicsDevice's default constructor evaluates GraphicsAdapter::getDefaultAdapterProperty() as a delegated-constructor argument, which is before createOrAttachWindow() acquires the video subsystem, so SDL_GetDisplays answers nothing and the fallback is cached for the life of the process. The evidence needs no SDL and no second process: in one frame with one device, cna_game_window_copy_screen_device_name answers the display's real name "screen" while cna_graphics_adapter_copy_description answers "Default Display" -- recorded as displayEvidenceConflict.adapterContradictsTheWindow. Initialising SDL's video subsystem before the device is constructed makes the same build report "screen" and 1280x800, which is the control. cna_graphics_adapters_refresh refuses by design, so no C consumer can correct it. Binding a Ruby GraphicsAdapter to the cached fallback would publish invented hardware. The two contract facts recorded here before are unchanged: the ABI answers adapter queries only through a callback-scoped graphics-device handle while XNA Adapters and DefaultAdapter are statics, and the native monitor handle is refused by design |
| `managed.not-supported-exception` | VERIFIED_MANAGED | verified projection | Foundation 30 resolved the decision mapping.not-supported-exception recorded. System.NotSupportedException maps to CNA::Runtime::NotSupportedError, a dedicated language-support class descending from StandardError. Ruby NotImplementedError is deliberately refused: it descends from ScriptError, not StandardError, so a bare rescue -- the analogue of a CLR catch (Exception) -- does not catch it, and a routine recoverable CLR failure would escape ordinary Ruby error handling; RuntimeError and ArgumentError are catchable but each say something the CLR exception does not and neither carries a distinct identity a caller can rescue by name. The admitted mscorlib settles the hierarchy, NotSupportedException under SystemException under System.Exception, which the register already roots at StandardError, and the three public constructor shapes, with the message forwarded and never synthesised; the parameterless form's CLR message is the localized framework resource Arg_NotSupportedException, which is not reproduced, so a NotSupportedError raised without a message carries Ruby's own default. The mapping lives in a third register, BclProjection::THROWN_EXCEPTIONS, kept separate from TYPES and EXCEPTION_BASES because a thrown exception is never named by an XNA public signature and must not count as a mapped BCL identity on the dependency frontier. The API verifier resolves every entry and reports LANGUAGE_MAPPING_MISMATCH for one that is not a class, is not a StandardError, or descends from ScriptError; five mutations are covered by test, including the NotImplementedError substitution |
| `bcl.stream-projection` | VERIFIED_MANAGED | projected and qualified against the pinned mscorlib measurement | System.IO.Stream projects to CNA::Runtime::Stream and System.IO.SeekOrigin to CNA::Runtime::Stream::SeekOrigin, both measured from the Microsoft .NET Framework 4.0 mscorlib admitted by exact SHA-256 rather than remembered. Seventeen XNA members reach System.IO and they reach it in both directions, so a produce-only projection would not have been enough: TitleContainer.OpenStream, ContentManager.OpenStream, StorageContainer.OpenFile/CreateFile and four Media getters produce one, while SoundEffect.FromStream, both Texture2D.FromStream overloads, Texture2D.SaveAsPng/SaveAsJpeg and MediaLibrary.SavePicture consume one. The CLR type is abstract and XNA never names a concrete stream, so there is exactly one Ruby class and where the bytes live is a private backing rather than a second projected identity; new is private for the reason Foundation 25 established. The projected surface is the reached surface: the four Begin/End asynchronous members, Synchronized, the Null field and the three family members are measured and deliberately absent, each because projecting it would mean inventing a type the inventory does not carry. SeekOrigin is the first transitively demanded BCL identity -- no XNA signature names it, System.IO.Stream::Seek does -- and it takes the binding's ordinary enum policy rather than a bag of Integers. System.Byte[] projects to a binary Ruby String, and the two properties a CLR array does not have, being resizable and being freezable, are refused rather than silently allowed. This claims no ContentManager, no StorageContainer, no content pipeline and no lazy I/O |
| `content.title-container` | VERIFIED_NATIVE | complete; qualified end to end against CNA's canonical title routes | Microsoft.Xna.Framework.TitleContainer, 1 type / 1 identity, the static class whose whole public surface is OpenStream(String). Every branch of its 213-byte IL is reproduced: the IsNullOrEmpty refusal, GetCleanPath, the IsCleanPathAbsolute rejection, and the catch that collapses FileNotFoundException, DirectoryNotFoundException and ArgumentException into one refusal -- which lines up exactly with CNA's own collapse of the same cases into CNA_RESULT_IO, chosen so a missing file does not surface as an internal failure, and which projects to Ruby's Errno::ENOENT. GetCleanPath and CollapseParentDirectory are transcribed including the loop whose IndexOf is its increment and whose index starts at 1, so a leading ..\ is never collapsed and is rejected instead; that is the traversal defence and it is tested case by case. The seven badCharacters are read out of the assembly's own fourteen-byte static blob and neither path separator is among them. Four canonical routes are bound and no adjacent storage API: the title location count/copy pair, the process-wide location override, and cna_title_container_read_ext. TWO DEVIATIONS, both recorded: CNA's title reader delivers the whole file through a count/copy pair because -- in its own header's words -- this ABI has no stream handle for title content, so the read is eager where XNA's FileStream is lazy and an I/O failure surfaces from OpenStream rather than from a later Read; and XNA's System.Uri validation step is not reproduced, because System.Uri lives in System.dll, which is not an admitted BCL authority here, and reconstructing which strings a relative Uri refuses would be a guess. Reading the file with Ruby's own File instead would be worse: it would bypass the runtime that owns the title location and could resolve a different path than cna_title_location_copy_path reports. This claims no ContentManager, no XNB support, no content pipeline and no StorageContainer |
| `content.content-manager` | VERIFIED_NATIVE | complete; Load<Texture2D> qualified against externally produced MonoGame .xnb fixtures | Microsoft.Xna.Framework.Content.ContentManager, 1 type / 10 identities, plus Game.Content -- Game's last missing member, so Game leaves the partial register as the first of the six deferred partial runtime types to do so. Two of the three BCL decisions NEXT.md listed turned out not to be BCL decisions: System.Action`1 structurally collapses to Ruby's #call, because across the whole measured XNA reach -- two public signatures -- every use is null-check, store and one invocation, with no Combine, no Remove, no comparison and no property exposing one; and !!0 is a generic method parameter, a language construct the frontier was reporting as an unmappable BCL type. The generic-method rule is that a CLR generic method takes its type arguments as leading positional Modules, which is the only rule under which a projected Ruby method may take more arguments than its CLR signature declares. ContentReader.InvokeReader<T> settles what the Action parameter means: it is an override for where a loaded disposable is registered, so a supplied callable replaces the manager's own bookkeeping rather than adding to it. Game.Content borrows the content manager CNA's game already owns -- one XNA ContentManager to one CNA content manager, the ownership rule the GraphicsDeviceManager producer audit established -- measured reachable before the first frame and outside every callback, answering the same handle every time. Caching is XNA's and measurement is why: CNA's typed loaders hand back a new independently owned handle per call, and CNA's normalized key case-folds without collapsing ./ or ../ where XNA's key is TitleContainer.GetCleanPath under an ordinal-ignore-case comparer. Load materializes through a registry naming exactly one supported T, Graphics.Texture2D, because it is the only XNA asset type this binding projects; every other T raises ContentLoadException. TWO DEVIATIONS: a standalone ContentManager cannot load, because cna_content_manager_create needs a graphics device that XNA finds through an IGraphicsDeviceService this binding deliberately registers no producer for, and its refusal names that; and ReadAsset does not route through OpenStream, because CNA's reader takes an asset name and no route in the ABI accepts .xnb bytes from a caller-supplied buffer, so OpenStream is projected and works but overriding it does not redirect Load. This claims no ContentReader, ContentTypeReader, content pipeline, XNB writer, SpriteFont, SoundEffect, Model or second asset type |
| `managed.resource-content-manager` | VERIFIED_MANAGED | complete; two identities, reaching nothing native | Microsoft.Xna.Framework.Content.ResourceContentManager, 1 type / 2 identities, and the first candidate this project's dependency frontier ever *selected* rather than merely listed: once System.Resources.ResourceManager was collapsed it had no blocker left, SELECTION_ROUTE became global-consumable-rank and SELECTED_NEXT named it. The collapse is the whole of the decision. The only member this type reaches is GetObject(string), whose answer it tests for null and then for byte[]; a .NET resource set is a satellite assembly's embedded .resources blob, which a Ruby program does not have and this binding will not invent, so the contract survives as any object answering GetObject -- the same shape System.Action`1 records for a callable -- and no Ruby constant is invented. The two ContentLoadException branches are different failures and are asserted to carry different messages: a name the set does not know, and a name whose value is not binary, which the System.Byte[] => String decision makes "not a String". The MemoryStream is CNA::Runtime::Stream.over_bytes, non-writable, which is what that projection exists for. The type reaches no native entry point -- the IL inventory measures that, and no route is bound for it -- and it keeps ContentManager's default empty root directory, which it never consults because OpenStream is overridden. Completing it implies no ContentReader, ContentTypeReader, ContentTypeReaderManager or XNB pipeline. |
| `bcl.action-and-generic-methods` | VERIFIED_MANAGED | decided and measured | System.Action`1 joins System.IServiceProvider and System.IDisposable in the structural-collapse register: it declares one member, Invoke(T), and Ruby's universal callable protocol is #call, so a Proc, a lambda, a Method and any object defining call all satisfy it. Mapping the identity to Proc would reject three of those four for no measured reason and inventing CNA::Runtime::Action would add an identity the CLR contract does not have. The whole XNA reach was measured before the decision -- two public signatures, every use null-check, store and one invocation -- so there is no delegate identity, no multicast, no removal and no equality to preserve. The API verifier asserts neither Action nor ActionOfT was invented, which required repairing that check for a generic identity: const_defined? raises on a name containing a backtick rather than answering false. Separately, a CLR generic method now projects to a Ruby method taking its type arguments as leading positional parameters, in declaration order, ahead of the CLR value parameters, each a Ruby Module because System.Type already projects to one; the rule lives in mapping-rules.json generics.methodProjection and is the only reason a projected Ruby method may take more arguments than its CLR signature declares. A generic parameter placeholder -- !!N for a method's, !N for a type's, either as an array -- is a language construct and is no longer counted as an unmapped BCL type; test_dependency_frontier.rb restates that rule independently, matching only the placeholder spelling so it can never excuse a real unmapped identity. System.Byte[] projects to a binary Ruby String, guarded at each boundary against the two properties a CLR array does not have. This claims no delegate system, no multicast, no generic type projection beyond the existing projects_elements metadata, and no blittable-array SetData/GetData support |
| `graphics.texture-collection` | VERIFIED_NATIVE | complete; both device collections bind and read real texture slots | Graphics.TextureCollection, 1 type / 1 identity -- the Item[Int32] indexer, get and set -- plus GraphicsDevice.Textures and GraphicsDevice.VertexTextures, which take the device's partial remainder down by two. Native frontier 4 recorded this type as the one case where NATIVE_RUNTIME was the right word, with no CNA route at all. That was simply mistaken, and not because the ABI moved: cna_graphics_device_get_texture and cna_graphics_device_set_texture are exported by the retired 0.7.0 artifact as well. It is the fourth frontier deferral this session to fail on inspection and the first that was wrong rather than reasoned from a wrong premise. The constructor is assembly, so new is private under Foundation 25's rule and a consumer reaches one only through the device; both device getters are one ldfld each, so each answers the same object every time and the two are distinct because they address different CNA_ShaderStage values where XNA distinguishes them by D3D9 sampler register base, 0 and D3DVERTEXTEXTURESAMPLER0. The bounds are the IL's: index < 0 or index >= maxTextures raises ArgumentOutOfRangeException(index) and an empty slot answers null. TWO DEVIATIONS: the getter answers a managed cache of what this collection bound, because CNA has deliberately no route from a native object back to a handle and its own header states the consequence and prescribes the cache, so a slot filled by canonical CNA code reads back as nil where XNA would answer the device's texture -- detectable rather than silent, because the slot descriptor's bound flag still reports it, which is asserted directly; and Length is CNA's 16 for both stages where XNA's is ProfileCapabilities.MaxSamplers and MaxVertexSamplers, which are profile-dependent and which this binding has no measured capability table for. Measured on the current artifact: a SpriteBatch flush leaves both views agreeing, because CNA unbinds what it bound, so the divergent case is real per CNA's header and not reachable from the surface this binding projects. This claims no SamplerState, SamplerStateCollection, Effect, render target, vertex or index buffer, and no draw member |
| `gamerservices.component` | VERIFIED_NATIVE | complete; the dispatcher is really pumped by the managed component engine | Microsoft.Xna.Framework.GamerServices.GamerServicesComponent, 1 type / 3 identities, the first type of a new namespace. Its frontier blocker was NATIVE_RUNTIME, which is not a blocker: every step of its IL has exactly one canonical CNA route, and -- unusually for this binding -- the dispatcher routes are process-global statics with no handle, which is what XNA's GamerServicesDispatcher is too, so the game-scoped asymmetry the audio and window families record does not apply. Only cna_gamer_services_dispatcher_initialize takes a game, and it takes it because the dispatcher adopts that game's service container, which is what XNA passes it. Initialize pushes Game.Window.Handle, subscribes to the title-update notification and initializes the dispatcher, in that order, with base.Initialize last; Update pumps the dispatcher then calls base.Update, so a running Game pumps it through the managed component engine without the consumer doing anything. The private InstallingTitleUpdate handler is not an identity and is not projected as one, but its effect is: the subscription is a real owned registration and the callback really calls Game.Exit. TWO DEVIATIONS: cna_gamer_services_component_create builds a canonical component whose initialize and update belong to CNA's runtime and which CNA's own component list drives, and it is deliberately unused because Game.Components here is the managed engine Foundations 35 and 38 built, so a canonical component would be driven twice -- the duplication the GraphicsDeviceManager producer audit refused; and XNA subscribes to a static event and never unsubscribes, where this releases the native registration on disposal, done through the type's own Disposed event rather than by declaring a Dispose the contract does not have. It also forced a verifier repair: xna_event_identities walks the whole ancestor chain, so this first type to inherit events rather than declare them was reported as registering three identities its contract does not select; the verifier now subtracts what the CLR base chain declares, read from the reference rather than from Ruby's ancestors, and a control asserts an invented event is still caught. This claims no Guide, Gamer, SignedInGamer, GamerProfile, NetworkSession or storage surface, and no gamer-services behaviour beyond pumping the dispatcher |
| `audio.dynamic-sound-effect-instance` | VERIFIED_NATIVE | complete; buffers really queue and the canonical pump really raises BufferNeeded | Microsoft.Xna.Framework.Audio.DynamicSoundEffectInstance, 1 type / 10 identities, the streaming instance. It reached the frontier only when the audio cluster completed SoundEffectInstance, which is its base, and its blocker was NATIVE_RUNTIME, which is not one. It is the one SoundEffectInstance with a public constructor because it has no SoundEffect behind it, which is also why cna_dynamic_sound_effect_instance_create is game-parented where an ordinary instance is effect-parented. The constructor bounds are the same IL literals SoundEffect carries. SubmitBuffer validates against the instance's own AudioFormat, and block alignment is the rule a summary would lose: the buffer length, the offset and the count must each be a whole number of frames -- two bytes for PCM16 mono, four for stereo -- which XNA reports as three different messages, InvalidAudioBuffer, InvalidAudioBufferOffset and InvalidOffsetCountLength, for what looks like one rule; alignment following the channel count is asserted directly, and PendingBufferCount is the runtime's own count rather than a managed tally, so a refused submission that had reached the runtime would show. IsLooped is the one member this type narrows rather than adds: set_IsLooped throws InvalidOperationException(InvalidDynamicIsLoopedCall) for true and accepts false, because a streaming instance has nothing fixed to loop over. Play primes the queue before starting. BufferNeeded is the first projected event whose raiser is the native runtime rather than this binding: CNA raises it from whichever thread advances the queue, and what advances it here is FrameworkDispatcher.Update, the member this binding already projects -- measured firing, with the instance as sender and EventArgs.Empty as argument. cna_dynamic_sound_effect_instance_update_ext is deliberately unbound for that reason: it is the per-instance half of the same pump, and binding it would give this binding two pumps for one queue. Disposal releases the buffer-needed registration before the base releases the instance, so the callback can never be raised against a handle that is already gone. NO AUDIBLE OUTPUT IS CLAIMED. This claims no AudioEngine, SoundBank, WaveBank, Cue, AudioCategory or Microphone |
| `audio.microphone-capture` | VERIFIED_NATIVE | complete; the device inventory is real and the managed contract is measured without opening a capture | Microsoft.Xna.Framework.Audio.Microphone, 1 type / 15 identities, the sixth frontier candidate whose NATIVE_RUNTIME deferral was not a blocker. This machine reports three capture devices and CNA names each one from the hardware -- "Default Device", "Ryzen HD Audio Controller Stereo Microphone", "Ryzen HD Audio Controller Digital Microphone" -- at 44100 Hz; the sixteen cna_microphone_* routes are declared identically by the retired 0.7.0 headers, which the ABI gate's cross-version check proves, so the classification was never about the ABI and never about the host. CNA has no microphone handle: every route is (CNA_Handle game, uint64_t index, ...), so the projection carries a BORROWED_EXTERNAL_SCALAR index, destroys nothing, and projects XNA's protected Finalize -- whose IL frees a handle -- as the member that does nothing. DEVIATION: XNA's collection is a CLR static and this one needs a live Game, the same asymmetry FrameworkDispatcher and the SoundEffect statics record. DEVIATION: the two conversion routes cna_microphone_get_sample_duration_ticks_at and cna_microphone_get_sample_size_in_bytes_at are bound but deliberately not used, because they measurably disagree with XNA: at 44100 Hz GetSampleSizeInBytes(100ms) is 8818 in XNA and 8820 in CNA, GetSampleSizeInBytes(1s) 88198 against 88200, GetSampleDuration(8818) 100 ms against 99, GetSampleDuration(46) 1 ms against 0. XNA truncates duration_ms * (float)(rate/1000f) toward zero through AudioFormat and rounds the other direction half away from zero through TimeSpan.FromMilliseconds, which the pinned mscorlib spells (long)(ms + 0.5) * 10000; CNA computes exactly and truncates. The arithmetic is therefore managed, in the IL's order, and a test asserts the divergence through private helpers rather than describing it. DEVIATION: IsHeadset answers true unconditionally because isHeadset has exactly one writer in the whole assembly, ldc.i4.1 in the constructor; CNA answers false for all three devices and the test asserts both. DEVIATION: CNA's set_buffer_duration_ticks_at accepts [100, 990] ms on a 10 ms boundary -- measured by sweeping every whole millisecond in [95, 1005] and every tick in [1000000, 1000020] -- against XNA's [100, 1000], so the single value XNA admits that CNA refuses is 1000 ms, which is also the value the getter reports on a device nothing has reconfigured. set(get()) therefore fails at the C ABI; classified UPSTREAM_CNA_CONTRACT and reproduced by a test. The projection sends 990 ms and caches the request, which is what XNA's cached-field getter answers. HARDWARE_PENDING, and deliberately so: Start, Stop and a real GetData read are implemented and left unexercised, because running them would record audio from the machine running the suite. The whole managed contract is reachable without that -- GetData's four-stage validation ladder ends in if (State != Started) return 0, which a stopped device answers -- so the refusals, the alignment rule and the rounds-to-zero-milliseconds clause that refuses every aligned count below 46 bytes at 44100 Hz are all measured. BufferReady is the second projected event whose raiser is the native runtime; the subscription is a real owned registration released with cna_audio_unsubscribe_ext. See docs/microphone-evidence.md. |
| `audio.xact-engine` | VERIFIED_NATIVE | complete; a real XACT project loads and every category resolves | Microsoft.Xna.Framework.Audio.AudioEngine and Audio.AudioCategory, 2 types / 24 identities. AudioCategory sat in the frontier's RUNTIME_DATA register with the reasoning "an XACT AudioEngine category handle; SetVolume/Pause/Resume/Stop act on a live engine this binding does not have". Both halves described the producer, and the second was measured false: CNA 0.21.0 loads the XNA Spacewar sample's SpaceWar.xgs for real -- six categories, eight variables -- resolves Global, Default, Music, Weapons and Ships by name, answers a real SDL3_mixer renderer, and round-trips SpeedOfSound from XACT's own 343.5 default. That is the sixth register entry retired for naming the producer, and the first retired because the claim stopped being true rather than because the reasoning was re-read. The fixtures are referenced by path through CNA_TEST_XACT_DIR and never copied into this repository, exactly as the MonoGame XNB fixtures are. The constructor validates the settings file by magic number in managed code before XACT is asked -- four bytes X G S F, and anything four bytes or shorter refused with ArgumentException(InvalidContentVersion) -- which the projection reproduces, so a .xsb handed to it is refused without the engine seeing it. DEVIATION: cna_audio_engine_create_with_renderer is game-parented where XNA's constructor needs no Game, the asymmetry every game-scoped audio route records. DEVIATION: CNA hands out a fresh category handle per cna_audio_engine_get_category call where XNA carries one uint16, so GetCategory caches by name -- which makes handle equality exactly XNA's _category ==, bounds the PARENT_OWNED handles the engine must release, and is asserted against CNA's own cna_audio_category_equals in both directions rather than trusted. A category handle is released by the engine that produced it, before the engine itself, because XNA's AudioCategory is a value type with no Dispose and no finalizer. DEVIATION: SetVolume's IL comparison is bge.un -- unordered -- so NaN takes the accepting branch, the exact opposite of SoundEffectInstance.Volume; the managed check is reproduced and CNA's own refusal of NaN is what surfaces. RendererDetails answers null rather than an empty collection when the renderer count is zero, because the IL's local starts as ldnull; this host has one renderer, so the branch taken is asserted and the null branch is not fabricated. Seven of the settings file's eight variables are cue-scoped rather than global, and XACT's refusal of them through GetGlobalVariable is measured as XACT's rather than reported as this binding's. No WaveBank, SoundBank or Cue is projected yet. See docs/xact-engine-evidence.md. |
| `audio.xact-banks-and-cues` | VERIFIED_NATIVE | complete; real banks load, a real cue plays, and the Audio namespace is finished | Microsoft.Xna.Framework.Audio.WaveBank, Audio.SoundBank and Audio.Cue, 3 types / 38 identities. Completing them completes the whole XNA 4.0 Audio namespace and empties it off the dependency frontier. Measured against the XNA Spacewar sample's real banks through CNA_TEST_XACT_DIR, never copied here: a 33-entry wave bank, a 30-cue sound bank, and the cue `menu_scroll`, which reports its own name back from XACT, is prepared when asked for, and is playing after Play plus one AudioEngine.Update. Each of the three checks its own magic number in managed code before XACT is asked anything -- W B N D for the wave bank, S D B K for the sound bank -- so handing either the wrong file raises ArgumentException(InvalidContentVersion) without the engine seeing it, and the fixtures really carry those bytes, which the test asserts rather than assumes. GetCue reports an unknown name as an ArgumentException and PlayCue reports the same underlying XACT error as an InvalidOperationException; both are the IL's and both are reproduced. Apply3D carries the same guard SoundEffectInstance does -- if (!applied3D && played) throw -- so the first apply must precede the first play and every later one is free, measured in both directions. The seven cue status properties are one XACT bitmask that CNA answers in CNA_CueInfo, whose 16-byte layout the ABI gate measures. DEVIATION: CNA refuses cna_sound_bank_destroy while any cue of that bank is alive -- "All C Cue children must be destroyed before their SoundBank" -- and refuses the engine while a bank is alive, where XNA leaves both to the CLR; so a bank disposes its cues first and an engine disposes its banks first, which is the enforced parent/child destruction the SoundEffect cluster already records, and the cascade is asserted end to end. DEVIATION: XNA's Cue.Play tolerates exactly XACT's 0x8ac70008 cue-instance limit and raises on anything else; CNA_Result has no code for it, so nothing is swallowed on a guess and every native failure raises. The exemption was measured unreachable on this artifact -- thirty consecutive plays of one cue all succeeded -- so no behaviour this artifact can produce is lost. HARDWARE_PENDING: no assertion is made about what is audible; what is measured is that the mixer opens at 44100 Hz stereo and the cue's own state machine moves. See docs/xact-banks-evidence.md. |
| `graphics.sprite-font` | VERIFIED_NATIVE | complete; a real 95-glyph font loads and every measurement agrees with CNA's own | Microsoft.Xna.Framework.Graphics.SpriteFont, 1 type / 6 identities, measured against MonoGame's Default.xnb -- an uncompressed SpriteFont already in the configured CNA_TEST_XNB_DIR -- through ContentManager.Load(SpriteFont, name), which is XNA's only producer and this binding's too. It is the first candidate a BCL decision alone unblocked: it carried both BCL_PROJECTION and NATIVE_RUNTIME, and every earlier doubly blocked candidate was resolved by building the type its IL reached, but SpriteFont's il-only dependency SpriteBatch had been complete all along. Three projections were decided for it. System.Char is an Integer UTF-16 **code unit**, not a one-character String: 0xD800 is a valid Char and not a valid Ruby character, so a String would force an encoding decision the CLR type does not have and could not hold an unpaired surrogate. Nullable`1 is nil-or-value, which needs no wrapper. System.Text.StringBuilder is a Ruby String, which is what makes the two MeasureString overloads collapse exactly rather than approximately. InternalMeasure is reproduced instruction for instruction, including the two details a paraphrase loses: the first glyph on a line clamps its left bearing to zero instead of paying the spacing, and the height comes from the cropping rectangle rather than the glyph bounds. Every measurement is asserted equal to cna_sprite_font_measure_utf8's rather than only to itself, and the font's character map is asserted sorted because GetIndexForCharacter binary-searches it. The fallback chain is measured in both directions: a missing character raises ArgumentException with no DefaultCharacter, measures as the fallback with one, and set_DefaultCharacter refuses a character the font does not define while clearing is always allowed. DEVIATION: LineSpacing= and Spacing= are bare field writes with no validation in XNA -- a negative line spacing is stored -- but cna_sprite_font_set_spacing documents "must be finite" and refuses NaN, so that refusal surfaces as CNA::NativeError rather than being hidden or reproduced managed-side. DEVIATION: XNA's SpriteFont is not IDisposable; its ContentManager records the Texture2D the reader built and disposes that. CNA hands back two owned handles and no projected Texture2D, so the font owns both and releases both, and the manager triggers it because it records anything answering Dispose. The widening is that a consumer can call font.Dispose; the safety is that a font whose atlas is gone reports IsDisposed. This milestone added no SpriteBatch.DrawString; the milestone after it did, and the Effect cluster later completed the type. See docs/sprite-font-evidence.md. |
| `graphics.device-manager-preferences` | VERIFIED_NATIVE | eleven of the twenty-six members the largest partial type owed; the rest is the producer audit's | Microsoft.Xna.Framework.GraphicsDeviceManager's nine preferred settings, ApplyChanges and ToggleFullScreen -- 20 Ruby identities, taking the type from 26 outstanding members to 15 and the project's MISSING_MEMBER from 107 to 96. They are preferences rather than device state: XNA's getters are one ldfld each and never consult the device, because PreferredBackBufferWidth is what the next device creation should aim for. So this projection keeps them in managed fields exactly as XNA does -- a consumer sets them before Run, when no native manager exists yet, which is the same reason XNA buffers them -- and pushes all nine into CNA's own manager when it appears and again at ApplyChanges. The defaults are the IL's own field initialisers, which run before Object::.ctor(): synchronizeWithVerticalRetrace true, depthStencilFormat Depth24 (ldc.i4.2), 800x480 from the two static defaults, everything else its CLR zero, and graphicsProfile from ReadDefaultGraphicsProfile -- which reads the manifest resource "Microsoft.Xna.Framework.RuntimeProfile" out of the game's own assembly and returns Reach when it is absent, as it always is in a Ruby program, by the IL's own not-found branch rather than by assumption. Only the two dimension setters validate, with ArgumentOutOfRangeException for a non-positive value; the other seven store and mark dirty with no validation, so what refuses a wrong value there is this binding's typing. Settings made before Run are asserted to reach CNA's manager once it exists, and ApplyChanges and ToggleFullScreen are asserted against what CNA reports back rather than only against the managed fields. DEVIATION: XNA's ChangeDevice creates the device when there is none; here device creation belongs to CNA's own manager, which is what docs/graphics-device-service-producer-audit.md found, so a pre-run ApplyChanges marks the settings pending instead of standing up a device -- asserted directly. The fifteen members still outstanding are exactly what that audit defers -- the four device events, their raisers, PreparingDeviceSettings and Dispose(Boolean) -- plus FindBestDevice, CanResetDevice and RankDevices, which name the missing GraphicsDeviceInformation. This milestone adds none of them, registers nothing in Game.Services, and claims no interface conformance: outcome B of that audit is untouched, and a test asserts it. |
| `graphics.resource-disposal` | VERIFIED_MANAGED | complete; the fifth partial type, and the disposal contract every graphics resource inherits | Microsoft.Xna.Framework.Graphics.GraphicsResource's three remaining members -- Dispose(Boolean), Finalize and the Disposing event -- which completes it and takes the partial count from five to four. The pinned Graphics assembly is mixed-mode C++/CLI, so the contract is spread over four methods: !GraphicsResource sets isDisposed; ~GraphicsResource returns early if it is already set, then calls !GraphicsResource and raises Disposing; Dispose(bool) picks between them; Dispose() is Dispose(true) plus GC.SuppressFinalize; Finalize is Dispose(false). Two things a paraphrase loses, and both are asserted rather than assumed: isDisposed is set **before** Disposing is raised, so a handler observes IsDisposed already true, and the finalizer path raises nothing at all -- only an explicit Dispose announces itself. Disposal is idempotent, which the isDisposed guard gives rather than a separate flag. Ruby cannot give one name two visibilities, so the public Dispose() and the protected Dispose(Boolean) project to one method with a default argument, the rule Game, ContentManager and every XACT type already follow, and no Ruby finalizer is registered -- the check is on code rather than on the word, because GC.SuppressFinalize is discussed in comments throughout. Texture2D and SpriteBatch each override Dispose(bool) in XNA, and each override's extra work is over state this binding does not have -- saved image data and a CleanupSavedData for one, an Effect and platform data for the other -- while the native handle is already released by each type's own NativeResource lambda. So the base behaviour is the whole of what remains and both members are selected rather than reimplemented, which a test asserts by checking that each inherits the base implementation. MISSING_MEMBER 96 -> 91 and OVERLOAD_MAPPING_MISMATCH 40 -> 36 across the two. |
| `graphics.texture-image-encoding` | VERIFIED_NATIVE | complete; real PNG and JPEG bytes, verified by their own signatures | Microsoft.Xna.Framework.Graphics.Texture2D.SaveAsPng and SaveAsJpeg -- the first members of this binding that produce a real encoded image, and the first consumer of the System.IO.Stream projection on the writing side. Both are one call into a private SaveAsImage(stream, format, width, height) with the format fixed -- ldc.i4.0 for JPEG and ldc.i4.2 for PNG -- and SaveAsImage validates exactly three things: a null stream (ArgumentNullException), a stream that cannot be written (ArgumentException), and a format that is neither of the two, which the public members cannot produce. There is **no width or height validation in the IL at all**. Measured against the 128x128 test PNG: the PNG output carries the exact eight-byte signature 89 50 4E 47 0D 0A 1A 0A and the JPEG output carries FF D8 FF, so what is asserted is a real encode rather than a plausible byte count. The stream is accepted on the same terms FromStream accepts a reader -- the projected Stream or any Ruby object answering write -- and CanWrite is consulted when the object has it, which is what XNA's check really is. DEVIATION: because XNA validates no size, a zero or negative target reaches the encoder; CNA refuses it and that refusal surfaces as CNA::NativeError rather than a managed rule being invented, while a 1x1 target really encodes. CNA's image-format identities are its own (PNG 0, JPEG 1) where XNA's private enum numbers them differently (JPEG 0, PNG 2), which is why the projection names the CNA constants rather than reusing XNA's literals. cna_texture2d_save_file is deliberately unbound: it writes a path, and XNA has no such identity. No pixel access is added -- SetData and GetData remain outstanding -- and nothing is drawn. |
| `graphics.texture-construction` | VERIFIED_NATIVE | complete; a texture can be made from nothing, with a real mip chain | Microsoft.Xna.Framework.Graphics.Texture2D's two public constructors -- the first way this binding makes a texture that did not come from an image or the content pipeline. XNA declares both public, unlike Texture and GraphicsResource whose constructors are assembly, so `new` is public here and stays private on the bases, which a test asserts on all three. Ruby has no overloading, so the two collapse into one method whose defaults are the shorter overload's own fixed CreateTexture arguments -- false and SurfaceFormat.Color -- rather than invented ones. Measured: a 4x3 texture reports 4x3 with one level and Bounds covering the whole surface, and an 8x8 asked for mipMap reports **four** levels, so the chain is real. A created texture then encodes to a valid PNG, which is what makes it a texture rather than a handle. CreateTexture refuses a null device with ArgumentNullException(DeviceCannotBeNullOnResourceCreate) and ValidateCreationParameters refuses a non-positive size with ArgumentOutOfRangeException(ResourceDimensionsMustBePositive) naming width or height; every later check in that method is a **profile capability** -- the maximum texture size the active GraphicsProfile allows -- which is a device fact rather than a managed rule, so it is CNA's to refuse and not this projection's to guess. DEVIATION: CNA's create route documents that its bulk-transfer slice supports CNA_SURFACE_FORMAT_COLOR, so Bgr565 is refused natively as CNA::CapabilityError where XNA would accept whatever the adapter supports; no managed rule is invented to anticipate it. SetData and GetData remain outstanding, so a created texture's pixels cannot yet be written or read -- only encoded. |
| `graphics.texture-pixel-access` | VERIFIED_NATIVE | complete; pixels round-trip exactly, sub-rectangles included | Microsoft.Xna.Framework.Graphics.Texture2D.SetData and GetData -- six overloads and the largest single derivation in the graphics half. All six are thin forwards into one 549-byte CopyData whose validation lives in four helpers, and every one is reproduced: Helpers.ValidateCopyParameters (a start index outside the array, a start plus count past its end, or a non-positive count -- and it names "dataIndex" for the first, not the public parameter's startIndex), Texture.GetAndValidateSizes (the element size must equal the format's byte size or divide it exactly), Texture.GetAndValidateRect (negative origin, non-positive extent, or reaching past the level), and Texture.ValidateTotalSize (the elements must exactly fill the region). Ruby cannot dispatch on parameter type, so the three overloads of each collapse into one method dispatching on **arity** with the generic type argument leading -- the rule ContentManager.Load established for !!0 -- and GENERIC_MAPPING_MISMATCH stays zero. Eleven element types are modelled, each mapped to its CNA_TEXTURE_DATA_* identity and the byte width sizeof(T) answers; System.Byte[] projects to a Ruby String, so ::String stands for byte. Every projected element type answers PackedValue, so one path packs and unpacks them all. Measured: four Colors written into a 2x2 texture come back with identical packed values and identical component bytes, and a 1x1 sub-rectangle written at (1,1) reads back exactly what was put there while the other three pixels stay untouched. DEVIATION: CopyData's remaining InvalidOperationException, CannotUseFormatTypeAsManualParameter, is reached by asking the **adapter** whether the element type suits the texture's format, and every adapter value is fabricated on the qualified artifact -- so that check is not reproduced from invented data and CNA's own refusal surfaces instead, which a test measures by asking a Color texture for byte data and getting "byte data requires ByteEXT format". The other, MustResolveRenderTarget, is unreachable because no render target is projected. Only FromStream's five-argument overload is left outstanding on this type. |
| `graphics.texture-from-stream` | VERIFIED_NATIVE | complete; the fourth partial type closed, and the first upstream defect this binding measures rather than hides | Microsoft.Xna.Framework.Graphics.Texture2D.FromStream's five-argument overload, which completes Texture2D -- COMPLETE_TYPES 161 -> 162, PARTIAL_TYPES 4 -> 3, MISSING_MEMBER 81 -> 80, OVERLOAD_MAPPING_MISMATCH 31 -> 30 -- and leaves GraphicsDeviceManager, GraphicsDevice and SpriteBatch as the only partial types left. Both XNA overloads forward to one internal constructor; the difference is a requested size and a `zoom` flag, cover-and-crop when true and fit-preserving-aspect when false. Ruby dispatches them on arity, and CNA carries the difference in CNA_Texture2DDecodeInfo, whose **null pointer is the two-argument case** -- so the collapse is the ABI's own shape rather than an invented one, and the 24-byte layout is measured by the ABI gate. The validation order is the IL's: null device, null stream, unreadable stream, then ValidateCreationParameters refusing a non-positive size, the same helper both public constructors call. Measured against the 128x128 test PNG: two arguments preserve 128x128; (64,64,false) and (64,64,true) both give 64x64; (64,32,false) gives 32x32, which is the fit rule doing arithmetic rather than obeying the request; (32,64,true) gives 32x64. UPSTREAM_CNA_DEFECT, reproduced at the C ABI with no Ruby in the path: the zoom path is asymmetric. From a square source a target taller than wide crops correctly, while one wider than tall -- (64,32,true), (200,100,true) -- fails INVALID_ARGUMENT with "ImageLoader: crop rectangle lies outside the source image". Cover-and-crop is symmetric by definition, so one orientation computes its crop rectangle wrongly. Nothing here works around it: substituting a fitted decode for a failed zoom would invent a result neither XNA produced nor CNA gave, so the failure is asserted as a failure and the test will report it if upstream fixes it. No CNA source was changed. See docs/texture-decode-upstream-defect.md. |
| `graphics.state-objects` | VERIFIED_MANAGED | complete; four types, and the ninth NATIVE_RUNTIME deferral that was not one | Microsoft.Xna.Framework.Graphics.BlendState, DepthStencilState, RasterizerState and SamplerState -- 4 types / 65 identities, taking COMPLETE_TYPES 162 -> 166, TARGET_MEMBERS 2014 -> 2079 and MISSING_TYPES 92 -> 88. All four arrived on the frontier the moment GraphicsResource completed and all four reported NATIVE_RUNTIME; the audit that word demands found the ninth instance of the same fallacy. What makes each native-reachable is `Apply`, which is `assembly`-visible in the pinned IL, is not in the contract, and is therefore not a projected identity -- the public surface is a constructor, an inherited Dispose, properties and static presets, managed to the last member. They are also the first GraphicsResource subclasses in this binding that own **no native handle and no device**: the IL's constructors call Object::.ctor() and then SetDefaults, so GraphicsDevice is genuinely null until Apply binds one, and the base gained a managed disposal flag rather than a handle's. Every default is the IL's SetDefaults read instruction by instruction, and each preset is one private constructor, so a preset differs from a fresh state only where its own constructor assigns -- asserted property by property rather than described. That makes new BlendState() value-identical to BlendState.Opaque, new DepthStencilState() to Default, new RasterizerState() to CullCounterClockwise and new SamplerState() to LinearWrap, which is XNA's arithmetic and not this projection's. isBound is what a preset sets and the setters check: a preset refuses every setter with InvalidOperationException -> RuntimeError naming its own type, and a fresh state accepts every one, measured in both directions for every property of all four types. CROSS-CHECK: every projected preset is asserted equal to CNA's own cna_*_state_init values field by field -- four routes that take no handle, so the whole check runs headless -- and the two authorities agree on 63 of the 65 values. UPSTREAM_CNA_DIVERGENCE on the other two: XNA's SetDefaults writes ldc.i4.m1 into StencilMask and StencilWriteMask, CNA answers Int32.MaxValue. -1 is every bit of the D3D9 mask DWORD and 0x7FFFFFFF drops the top one, so on an 8-bit stencil buffer nothing rendered differs -- but the value a consumer reads back does, and the pinned IL is the authority, so the projection answers -1 and the divergence is asserted in both directions rather than reconciled. DEVIATION: the setters refuse by type where the IL validates nothing at all, which is this binding's enum projection rather than XNA's rule. DEVIATION: no instance is frozen and Name stays settable on a preset, because GraphicsResource::set_Name carries no bound check and a frozen object would refuse Tag and disposal too. This milestone leaves every GraphicsDevice state property outstanding, projects nothing that holds a collection of these states, and adds no state-bearing SpriteBatch.Begin: cna_graphics_device_get/set_*_state and cna_sprite_batch_begin_with_states are not bound by it, and nothing here draws. See docs/graphics-state-objects-evidence.md. |
| `graphics.sampler-state-collection` | VERIFIED_NATIVE | complete; the first frontier blocker in a while that was right, and right is what made it buildable | Microsoft.Xna.Framework.Graphics.SamplerStateCollection -- 1 type / 2 identities -- plus GraphicsDevice.SamplerStates and VertexSamplerStates, taking that partial type's remainder from 39 to 37 and the project's MISSING_MEMBER from 80 to 78. It reached the frontier the moment SamplerState completed, the uncovering a completed base always causes, and it carried NATIVE_RUNTIME. Unlike the four state objects the word was accurate here -- set_Item really does call SamplerState::Apply, which reaches the device -- and being accurate is what made the type buildable rather than blocked, because cna_graphics_device_set_sampler_state is precisely the route that apply needs. Three details the IL carries that a paraphrase loses, each measured: the getter answers the **cache**, not the device, so it asks CNA nothing; the setter refuses null with ArgumentNullException where TextureCollection's accepts it to unbind, which is a real difference between the two collections and is asserted in both directions; and the short-circuit is **reference** equality, so re-assigning the same object does nothing while assigning a distinct but value-identical state does reach the device -- the half a `==` short-circuit would get wrong, measured directly. Both device getters are one ldfld, so each answers the same object every time and the two differ by CNA shader stage, the same analogue of D3D9's sampler register base that TextureCollection records. DEVIATION: XNA's InitializeDeviceState -- assembly, run when the device is created or reset -- applies SamplerState.LinearWrap to all sixteen slots and leaves the cache holding it. This binding does not own device creation, and applying sixteen states from a property getter would be a write XNA performs elsewhere, so the cache is seeded with the same object and the claim that the device really is in that state is measured through cna_graphics_device_get_sampler_state: all sixteen slots of both stages report exactly LinearWrap's filter and address before anything is written. MEASURED: CNA validates struct_size and struct_version on the way **in** to a get route, so a descriptor handed to one must already carry them -- the four state layouts now fill their own header. GraphicsDevice's three state properties stay outstanding: each setter ends an active EffectPass, and no Effect is projected. Nothing here draws. |
| `graphics.vertex-declaration` | VERIFIED_MANAGED | complete; the only candidate that ever carried two blockers, and both named members the contract never selects | Microsoft.Xna.Framework.Graphics.VertexDeclaration and Graphics.IVertexType -- 2 types / 6 identities, COMPLETE_TYPES 167 -> 169 and MISSING_TYPES 87 -> 85. VertexDeclaration reached the frontier with NATIVE_RUNTIME **and** INTERFACE_PRODUCER_MISSING, the only dependency-complete candidate that ever carried the second. Both named members the pinned contract never selects: the reachability is Bind/Unbind, both assembly and reaching DeclarationManager, and the only caller of IVertexType::get_VertexDeclaration is the assembly static FromType. That is the tenth deferral retired this way, and it emptied INTERFACE_PRODUCER_MISSING off the dependency-complete list entirely. The public surface is two constructors, VertexStride, GetVertexElements and the inherited Dispose -- like the four state objects, a GraphicsResource with no native handle and no device, because the IL's constructors call Object::.ctor and then validate. `params VertexElement[]` means both call shapes compile in C#, so both are accepted and the two overloads collapse on whether the first argument is the stride, with no OVERLOAD_MAPPING_MISMATCH entry. Two details a paraphrase loses, both measured: an **empty** array raises ArgumentNullException rather than ArgumentException, and the elements are cloned on the way in *and* on the way out -- so mutating the caller's array or the one GetVertexElements answers changes nothing. Ruby's VertexElement is an object where XNA's is a struct, so both copies are element-wise. VertexElementValidator.Validate is reproduced in the IL's order -- non-positive stride, stride not a multiple of four, usage outside 0..12, offset outside the stride, offset not a multiple of four, duplicate usage/index pair, and byte-range overlap against an occupancy map -- and each refusal is asserted at the point the IL raises it, including that the outside-stride check precedes the offset-alignment one. The usage-range check is unreachable from Ruby, because VertexElementUsage is a projected enum that refuses anything outside its thirteen, and that is recorded rather than reproduced. CROSS-CHECK: GetVertexStride is a **maximum** of Offset + GetTypeSize(Format) rather than a sum, so elements may be declared in any order and holes are kept -- every stride and all twelve format sizes are asserted equal to CNA's own cna_vertex_declaration_create + get_stride, four routes that take no device and no game. IVertexType is projected as an abstract contract and **nothing conforms to it**: XNA's implementers are the vertex structs, which this milestone does not build, so a completed interface is still not a provider -- which is what the producer rule measures and what the frontier then confirmed by listing those four structs as consumable. See docs/vertex-declaration-evidence.md. |
| `graphics.vertex-structs` | VERIFIED_MANAGED | complete; the first types here that conform to a projected interface, and the first work queue this frontier has consumed | Microsoft.Xna.Framework.Graphics.VertexPositionColor, VertexPositionTexture, VertexPositionColorTexture and VertexPositionNormalTexture -- 4 types / 38 identities, COMPLETE_TYPES 169 -> 173, TARGET_MEMBERS 2088 -> 2126, MISSING_TYPES 85 -> 81. The dependency frontier ranked all four consumable with no blocker at all and SELECTED_NEXT named the first; this is that selection acted on, and it took the queue back to empty -- the first time this frontier has had one and consumed it. Nothing arrived behind them, because they are leaf value types: everything they name was already complete. They are the first types in this binding that **conform** to a projected interface rather than declaring one, so IVertexType has real producers and the only producerless interface left on the report is IGraphicsDeviceService. XNA implements IVertexType.VertexDeclaration **explicitly** -- private hidebysig newslot specialname virtual final in the metadata, so v.VertexDeclaration does not compile on the concrete type in C# either -- and the Ruby analogue is a private instance method, while the public name of the same spelling is the static initonly field, which projects to a constant; both halves are asserted, including that calling the public spelling raises NoMethodError. Each static declaration is its class constructor's own VertexElement literals with Name set afterwards, and the strides -- 16, 20, 24, 32 -- are what VertexDeclaration's own arithmetic computes rather than numbers written down twice. GetHashCode is Helpers.SmartGetHashCode over the sequential layout: XOR every complete 32-bit word, substitute Int32.MaxValue for zero, a Vector contributing its binary32 bits and a Color its packed value -- so all four all-default values answer 2147483647, and the XOR is asserted word by word for the four-word and eight-word cases. Equals(object) is exact-typed, so a VertexPositionTexture never equals a VertexPositionColor. Every field copies on the way in, because Ruby's Vector3 and Color are objects where XNA's are structs. The helper module lives in CNA::Runtime rather than the XNA namespace, which the verifier measures as INTERNAL_TYPE_LEAK and this binding has got wrong before. This milestone builds no VertexBuffer, no Effect and no draw call of its own; the Effect cluster came later and completed SpriteBatch with it. |
| `media.video-player` | VERIFIED_NATIVE | complete; the eleventh NATIVE_RUNTIME that was not one, and the frontier is at rest | Microsoft.Xna.Framework.Media.VideoPlayer -- 1 type / 15 identities, COMPLETE_TYPES 173 -> 174 and MISSING_TYPES 81 -> 80, taking the dependency frontier to three candidates every one of which has now been audited and stayed deferred. It was the last unaudited candidate and the one most likely to be blocked for the reason its column reported: fifteen members are native-reachable in the pinned Video assembly (SHA-256 17538b1c…), including the constructor, GetTexture and every transport control. The audit found the word wrong for the eleventh time. Every one of the fifteen has a canonical CNA route and all of them work headless, and the optional video decoder -- which cna_video_create's own header warns may be absent -- really is compiled into the qualified artifact: a real AVI already on this host is probed for its real metadata (320x240, 12.048193 fps, 6.557 s), Play moves the state machine to PLAYING, and a frame texture exists. That was measured at the C ABI with no Ruby in the path and with the file referenced by path, never copied, exactly as the XACT and XNB fixtures are. Every public member follows one IL shape -- lock, ThrowIfDisposed, then a decoder call guarded by IsValidDecoder and a non-null active video -- with one asymmetry that is XNA's and is measured in both directions: the getters for IsLooped, IsMuted and Volume are a bare ldfld outside the lock with no disposal check, so they answer after Dispose, while their setters take the lock and throw. set_Volume's range check ends in ble.un -- unordered -- so NaN takes the accepting branch, the same asymmetry AudioCategory.SetVolume records and the opposite of SoundEffectInstance.Volume. GetTexture is the one member that throws rather than returning when nothing is playing, and the IL constructs a **bare** InvalidOperationException with no message. DEVIATION: cna_video_player_create is game-parented where XNA's constructor takes nothing, the asymmetry every game-scoped audio and window route records. DEVIATION, measured rather than worked around: **nothing in this binding produces a Video.** XNA's only producer is ContentManager.Load<Video> and CNA exports no content route for video, so Media.Video has been producerless since Foundation 52 and Play cannot be handed a legal argument. Play reproduces XNA's ArgumentNullException("video") and then refuses explicitly, naming the missing producer, rather than inventing a Video factory XNA does not declare. HARDWARE_PENDING: nothing is claimed about anything visible -- what is measured is that a frame texture exists, not what is in it. See docs/video-player-audit-evidence.md. |
| `graphics.sprite-batch-draw-string` | VERIFIED_NATIVE | complete; the first member here that draws text, and what the SpriteFont milestone was for | Microsoft.Xna.Framework.Graphics.SpriteBatch.DrawString -- six CLR overloads, taking MISSING_MEMBER 78 -> 72 and OVERLOAD_MAPPING_MISMATCH 30 -> 29, and leaving Begin and Draw as the only members that type still owes. The IL validates exactly two things -- ArgumentNullException("spriteFont") and ArgumentNullException("text") -- and then forwards to SpriteFont.InternalDraw; the interval rule lives in that forward rather than in this member. Six overloads collapse to two Ruby arities. The StringBuilder half of each pair is already the same call, because System.Text.StringBuilder projects to a Ruby String -- the decision SpriteFont.MeasureString recorded -- and the two nine-argument shapes differ only in whether scale is a Single or a Vector2, which the IL's own Single form settles by loading the value into both components of one Vector2 local. So one method reads the argument it was given and does the same, and the collapse costs one OVERLOAD_MAPPING_MISMATCH entry rather than adding any. Measured against MonoGame's real Default.xnb through ContentManager.Load(SpriteFont): every arity submits inside a Begin/End interval, an empty string is accepted, a Single scale and the equivalent Vector2 are the same call, and both null checks raise where the IL raises them. DEVIATION: the interval rule is this binding's CNA::InvalidBindingStateError, the same one Draw uses, asserted both before Begin and after End. DEVIATION: non-finite transforms are refused by this binding, as every SpriteBatch submission is. MEASURED: a character the font cannot resolve is refused by CNA carrying the message XNA's own InternalDraw raises for the same reason -- the font really is the 95-glyph printable ASCII range with no DefaultCharacter, so nothing substitutes. cna_sprite_batch_draw_mesh_ext stays unbound (no XNA identity), and so do begin_with_states and begin_with_effect: the state-bearing Begin overloads are still outstanding. HARDWARE_PENDING: nothing is claimed about what is visible -- HEADLESS qualifies submission, not rendered text. |
| `graphics.sprite-batch-begin-states` | VERIFIED_NATIVE | complete for the three overloads that need no Effect; the fourth upstream defect this project has measured | Microsoft.Xna.Framework.Graphics.SpriteBatch.Begin's two state-bearing overloads -- (sortMode, blendState) and the five-argument one -- taking MISSING_MEMBER 72 -> 70 and leaving SpriteBatch owing only Begin's two Effect-taking overloads and Draw's three. All five XNA overloads forward to the seven-argument one, which stores its arguments **nulls included** and lets SetRenderState substitute at apply time: BlendState.AlphaBlend, SamplerState.LinearClamp, DepthStencilState.None and RasterizerState.CullCounterClockwise. cna_sprite_batch_begin_with_effect is the route that shape maps to, and its header documents exactly those four defaults for a null descriptor -- two independent authorities agreeing on the values. UPSTREAM_CNA_DEFECT: the route does **not** honour that documented null. A null descriptor is refused INVALID_ARGUMENT with "The BlendState descriptor is invalid", one parameter at a time in declaration order, and cna_sprite_batch_begin_with_states refuses it the same way -- reproduced at the C ABI with no Ruby in the path. The projection therefore resolves the four defaults itself, which is **not** a workaround dressed up: SetRenderState performs that exact substitution in the IL, so passing the resolved state reproduces XNA rather than compensating for CNA, and the values are the projected presets the state-object milestone already cross-checked against cna_*_state_init. The raw route is asserted to still refuse the null, so the record stays honest if CNA changes. The zero-argument overload keeps its own canonical route, cna_sprite_batch_begin. The four state objects are now consumed by something other than themselves, and the descriptor a state writes is one mapping with two callers -- this member and SamplerStateCollection. This milestone builds no Effect, no transform matrix and no GraphicsDevice state property of its own; the Effect cluster later supplied the first two and completed the type. See docs/sprite-batch-begin-upstream-defect.md. |
| `graphics.sprite-batch-draw-stretched` | VERIFIED_NATIVE | complete; SpriteBatch.Draw is finished and only the two Effect-taking Begin overloads are left | Microsoft.Xna.Framework.Graphics.SpriteBatch.Draw's three destination-rectangle overloads, taking MISSING_MEMBER 70 -> 67 and OVERLOAD_MAPPING_MISMATCH 29 -> 28 and completing that member. SpriteBatch now owes only Begin's two Effect-taking forms. XNA's seven Draw overloads split on the second parameter: four take a Vector2 **position** and are scaled from it, three take a Rectangle **destination** and are stretched into it -- which is why the stretched forms carry no scale at all and the longest of them has eight parameters where the position form has nine. CNA carries the same split in two structures and two submit routes, CNA_SpriteCommand with cna_sprite_batch_submit_many beside CNA_SpriteScaledCommand with submit_scaled_many, so the projection dispatches on the second argument's runtime type the way the C ABI already does rather than inventing a shape -- and the test asserts the split from the pinned contract as well as from the two layouts. Measured against the 128x128 test PNG inside a real interval: all three shapes submit, a null source rectangle is the whole texture exactly as it is for the position overloads, and both families work in the same Begin/End interval. DEVIATION: non-finite transforms are refused by this binding, as every SpriteBatch submission is, and the interval rule is the same CNA::InvalidBindingStateError Draw and DrawString already use. cna_sprite_batch_draw_mesh_ext stays unbound -- no XNA identity. Nothing is claimed about what is visible. |
| `graphics.vertex-index-buffers` | VERIFIED_NATIVE | complete for all five types; the strided window is a measured mapping limitation | Microsoft.Xna.Framework.Graphics.VertexBuffer, DynamicVertexBuffer, IndexBuffer, DynamicIndexBuffer and VertexBufferBinding -- 43 identities, taking the strict scoreboard 186 complete / 69 missing to 191 / 64 and TOTAL_DIAGNOSTICS 162 to 157, with MISSING_MEMBER unchanged at 65 because every one of the five is complete on arrival. A VertexBuffer owns a native declaration of its own: this binding's VertexDeclaration is managed-only with no handle, so the buffer builds one from the managed declaration's elements and its own stride through cna_vertex_declaration_create_with_stride and destroys it after itself -- proved by the second destroy of that handle being refused. The size rules are VertexBuffer::CopyData's own: a non-zero vertexStride must be at least sizeof(T) (VertexStrideTooSmall), the span a copy touches is elementCount * sizeof(T) + (elementCount - 1) * (vertexStride - sizeof(T)), and offsetInBytes + span must fit or the IL throws ResourceDataMustBeCorrectSize; a zero stride is tightly packed rather than the declaration's stride. IndexBuffer::CopyData carries the simpler half of the same rule. The overloads are told apart by the first parameter's type, the way the CLR tells them apart, because (offsetInBytes, data, startIndex, elementCount) and (data, startIndex, elementCount, options) are both four arguments; SetDataOptions is refused on a static buffer, which has no such overload. MAPPING LIMITATION, measured at the C ABI with no Ruby in the path: CNA's raw routes accept only the buffer's own declaration stride -- "The vertex stride does not match this VertexBuffer's VertexDeclaration" -- and address whole vertices, so a tightly packed window covering whole vertices from a vertex-aligned offset is passed through as exactly that transfer, while XNA's strided window (one component per vertex) raises CNA::Runtime::NotSupportedError, after the IL's own size rule has been checked. DEVIATION, recorded: the option-bearing DynamicVertexBuffer.SetData uses CNA's typed route, because the only raw route carrying both a byte offset and the options exists in 0.21.0 alone and binding it would end the retired 0.7.0 headers' admission; it therefore carries only the four projected vertex layouts where XNA accepts any struct, and the inherited option-free overloads keep the raw route and every element type. DEVIATION, recorded: ContentLost is subscribable and never fires -- is_content_lost measures false on all three qualified artifacts, so cna_vertex_buffer_subscribe_content_lost and its index twin stay unbound rather than delivering nothing. VertexBufferBinding's three values are read back from the structure cna_vertex_buffer_binding_init fills, and it is the one value type here that declares no Equals and no GetHashCode, so hash is answered over its components and no GetHashCode identity is published. Fifteen routes to 349, four constants to 133, seven layouts to 57, ABI_MISMATCHES=0. Nine of ten planted defects are caught by the suite beside it. This milestone draws nothing: GraphicsDevice defines none of the five Draw* members and no primitive or topology constant is bound. See docs/vertex-index-buffer-evidence.md. |
| `graphics.directional-light` | VERIFIED_MANAGED | complete; three types, no route bound, and the frontier's fewest candidates ever | Microsoft.Xna.Framework.Graphics.DirectionalLight, EffectMaterial and the IEffectLights contract they unblocked -- 12 identities, taking the strict scoreboard 191 complete / 64 missing to 194 / 61 and TOTAL_DIAGNOSTICS 157 to 154. Both classes sat on the frontier under NATIVE_RUNTIME and both were that word's twelfth and thirteenth false positive: every native edge the light has is EffectParameter::SetValue(Vector3), and the material's single constructor is `ldarg.0; ldarg.1; call Effect::.ctor(Effect); ret`. Both of those are projected, so this milestone binds no route, adds no constant and no layout, and the light holds no handle and has no Dispose. The IL's asymmetry is projected exactly and asserted from the parameters' side: the direction is written whether the light is enabled or not while the two colours are written only while it is enabled; set_Enabled returns untouched on an unchanged write and otherwise pushes either the cached colours or Vector3.Zero into the parameters; and the constructor's two paths differ in kind -- with no cloneSource it runs its own setters with Down/One/Zero, so only the direction reaches a parameter, and with one it copies fields and writes nothing at all. Every parameter may be null and every call site checks, which is what a BasicEffect whose shader declares no specular parameter really constructs. Measured over FNA's own BasicEffect.fxb, referenced by path through CNA_TEST_FX_BASIC and never copied into this repository: it is the effect XNA builds a DirectionalLight over and the only fixture here that declares float3 parameters, where the conformance effect's only vector parameter has four columns and GetValueVector3 refuses it as XNA does. IEffectLights is an abstract contract like the other four interfaces -- every member raises NotImplementedError and DirectionalLight deliberately does not include it, because the light is what an implementer hands out rather than an implementer. dependencyCompleteCandidates falls 5 to 3, the fewest this frontier has ever carried, and the three stock effects that name the light join partialDependencySatisfied instead. Nine of nine planted defects are caught. No stock effect is built: all five need cna_effect_matrices_set_world/_view/_projection, which take CNA_Matrix by value -- a 64-byte MEMORY-class aggregate Fiddle cannot pass, with no pointer variant. See docs/directional-light-evidence.md. |
| `graphics.render-targets` | VERIFIED_NATIVE | complete for all three types; one upstream defect measured and pinned | Microsoft.Xna.Framework.Graphics.RenderTarget2D, RenderTargetCube and RenderTargetBinding -- 22 identities, taking the strict scoreboard 194 complete / 61 missing to 197 / 58 and TOTAL_DIAGNOSTICS 154 to 151, with event identities 29 to 31 across 18 owners. A render target is a texture on both sides of the boundary: XNA derives the two classes from Texture2D and TextureCube, and CNA's render-target handle answers cna_texture2d_get_data, which is what the renderer qualification has been reading back since Native frontier 6. Each type is therefore its texture base with cna_render_target2d_create / cna_render_target_cube_create as creator and cna_render_target_destroy as destroyer. The constructor negotiates rather than records: XNA passes the preferred format, depth format and sample count through GraphicsAdapter.QueryFormat and stores the answers in a RenderTargetHelper, CNA negotiates in the same place inside its create route, and this projection reads all four values back from cna_render_target_get_info -- which is why the missing GraphicsAdapter did not block either type, and why both were built with two frontier blockers still unresolved. UPSTREAM_CNA_DEFECT, reproduced at the C ABI with no Ruby in the path: cna_texture2d_set_data over a render-target handle returns CNA_RESULT_SUCCESS and writes nothing, while the same call over a plain Texture2D handle round-trips exactly; readback is unaffected. The projection passes SetData through rather than inventing a refusal XNA does not have, and the test pins both halves. DEVIATION, recorded: ContentLost is subscribable and never fires -- is_content_lost is false on every renderer family that cannot lose a device, and the subscribe route exists only in 0.21.0, so binding it would deliver nothing and end the retired headers' admission. IsContentLost keeps the IL's latch. RenderTargetBinding is assembled in Ruby rather than by CNA, because no init route exists for it: its C structure is only consumed by cna_graphics_device_set_render_targets, a GraphicsDevice member this binding has not projected. Four routes to 353 and three layouts to 60, ABI_MISMATCHES=0, no new constant. Nothing here can bind a target: SetRenderTarget and its siblings stay unbound. Seven of eight planted defects are caught and the survivor is an equivalent mutant. See docs/render-target-evidence.md and docs/render-target-upload-upstream-defect.md. |
| `graphics.occlusion-query` | VERIFIED_NATIVE | complete; the frontier's partial list is down to stock effects | Microsoft.Xna.Framework.Graphics.OcclusionQuery -- 6 identities, taking the strict scoreboard 197 complete / 58 missing to 198 / 57 and TOTAL_DIAGNOSTICS 151 to 150. The whole type is the state machine its five fields make, and the rule a reader would not guess is at both ends of it: the constructor sets _hasIsCompleteBeenQueried true, which is what lets the first Begin through, and Begin clears it, so every later Begin needs an IsComplete read in between. IsComplete is therefore not a pure query -- it records that it ran even on the paths that answer without asking the device, which are "no native object" and "no Begin yet". Seven routes, all declared by both admitted header versions; has_renderer is production surface rather than a probe, because XNA's get_IsComplete asks exactly that question of pComPtr first. cna_occlusion_query_get_is_pixel_count_precise_ext stays unbound: it answers a real question -- OpenGL ES 3.0 and WebGL 2 count "any or none", so a coverage ratio from a boolean count is 1/area rather than a fraction -- but XNA has no identity for it. DEVIATION, recorded: XNA refuses construction on a GraphicsProfile whose ProfileCapabilities.OcclusionQuery is false, which is Reach; that is a device capability rather than a managed rule, and CNA answers it from the backend with CNA_RESULT_NOT_SUPPORTED rather than this projection inventing a profile table. Measured on both real artifacts and HEADLESS: the shape is the same and the timing is not -- HEADLESS completes on the first read with a count of 1, OPENGL33 is asynchronous and settles within a few reads counting 0 for an interval that drew nothing. Six of seven planted defects are caught; the survivor removes the _hasCalledBegin short-circuit and is an equivalent mutant on every qualified artifact, because the route beneath it answers "not complete" for a query that has never run -- which the test now asserts. See docs/occlusion-query-evidence.md. |
